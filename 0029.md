# 0029 — 사진 촬영 인증 게이트: 펫별 1·4·10번째 글

> 작성: 2026-07-28. 대상: `public.create_post_verified`, `pets.verify_post_count`,
> 앱 `post_create_screen`. 0018(사진 실존 검증)·0019(개체 인증)·0020(신원 인증)의
> **게이트 정책만** 바꾸는 문서다. 촬영·AI 대조 파이프라인 자체는 그대로다.

## 0. 결정값

| 항목 | 값 |
| --- | --- |
| 인증 요구 시점 | 펫별 검증 카테고리 글의 **1·4·10번째** (총 3회) |
| 11번째 글부터 | 요구 없음(영구 면제) |
| 판정 근거 | `pets.verify_post_count` (그 펫의 검증 카테고리 글 누적 수) |
| 판정 함수 | `app.needs_photo_gate(count)` = `count in (0,3,9)` |
| 누적 갱신 | `post_pets` AFTER INSERT 트리거(`trg_post_pets_bump_verify_count`) |
| 검증 카테고리 | `walk_together` · `walk_proxy` · `care` · `give_away` |
| 여러 마리 연결 | 하나라도 인증 순번이면 그 글은 인증 필요 |
| 촬영 대상 | 연결한 펫 중 아무나(2026-07-20 완화 유지) |
| 게시글 삭제 | 누적 수는 줄지 않음(지웠다 다시 쓰기로 게이트 회피 불가) |

## 1. 왜 바꿨나

기존 규칙은 **`pets.trust_score >= 3` 이면 면제**였고, `trust_score` 는 그 펫이 연결된
게시글의 **약속이 완료되고 후기가 작성될 때** +1 이었다(0019, `tg_reviews_grant_pet_trust`).
문제는 둘이었다.

1. **면제가 사실상 오지 않는다.** 약속 성사 → 완료 → 후기까지 세 번을 채워야 해서,
   대부분의 사용자에게는 "글 쓸 때마다 매번 촬영"이 체감 규칙이었다.
2. **규칙이 어디에도 안 보인다.** 작성 화면에는 촬영을 요구하는 문구만 있고 "언제까지
   해야 하는지"가 없었다. 사용자 피드백도 여기서 나왔다 — *"이걸 작성할 때마다 계속
   해야 하나?"*

바꾼 규칙은 **횟수를 줄이되(3회) 시점을 흩뿌린다**. 첫 글에서 실존·동일개체를 확인하고,
4·10번째에 표본 재확인을 한다. 계정이 오래됐다고 무한 신뢰하지 않으면서도 사용자는
"세 번만 하면 된다"를 명확히 안다.

`trust_score` 는 유지한다(신뢰도·배지 표시). **게이트 판정에는 더 이상 쓰지 않는다.**

## 2. 스키마

```sql
alter table public.pets add column verify_post_count int not null default 0;
grant select (verify_post_count) on public.pets to anon, authenticated;  -- 조회만
```

기록은 트리거(definer)만 한다 — 0019 의 `pet_match_count` 와 같은 원칙(사용자 편집
컬럼 화이트리스트에 넣지 않음)이라 클라이언트가 순번을 조작할 수 없다.

백필은 **삭제된 글 포함** 전체 이력 기준으로 한 번 수행했다.

## 3. 판정과 갱신

```sql
create function app.needs_photo_gate(p_verify_post_count int) returns boolean
  immutable as $$ select coalesce(p_verify_post_count,0) in (0,3,9) $$;
```

- 인자는 **지금까지 올린 글 수** = 이번 글 순번 − 1.
- `create_post_verified` 는 연결 펫 중 하나라도 `needs_photo_gate` 면 토큰 검증 경로로,
  아니면 `app.photo_trusted` 를 세워 `tg_posts_check_write` 의 사진 요구를 건너뛴다
  (기존 우회 플래그 그대로 — 트리거는 손대지 않았다).
- 누적 가산은 RPC 가 아니라 **`post_pets` 트리거**에 뒀다. RPC 밖 경로(직접 INSERT)로도
  글이 생기면 순번이 새기 때문 — 0026 의 "불변식은 트리거로" 원칙과 같다.

## 4. 앱 (pmdart)

- `MyPet.verifyPostCount` 추가, `MyPet.photoGatePostNos = [1,4,10]` 이 규칙의 앱쪽 정본.
  `needsPhotoGate` / `nextGatePostNo` 로 판정·안내를 만든다. **서버와 규칙이 갈리면
  앱은 "인증 불필요"로 그려 놓고 서버가 거부**하므로 둘을 같이 바꿔야 한다.
- 작성 화면(`_PhotoGateNotice`): 규칙 한 줄 + 선택한 펫별 진행도를 항상 노출한다.
  - `콩이 · 이번이 1번째 글이라 촬영 인증이 필요해요`
  - `두부 · 이번이 5번째 글이라 인증 없이 올려요 (다음 인증은 10번째)`
  - `콩이 · 인증 3번을 모두 마쳤어요. 이제 촬영 없이 올릴 수 있어요`
- 펫 선택 타일의 `인증 면제` 배지는 이제 순번 기준(`!needsPhotoGate`)이다.

## 5. 테스트

- `supabase/tests/t16_photo_gate_milestones_test.sql` — 판정 함수 5건 + 첫 글 거부/성공,
  누적 +1, 2번째 면제, 4번째 재요구.
- `supabase/tests/t06_post_photo_target_test.sql` — 촬영 대상 완화(연결 펫 중 아무나)
  회귀 가드를 새 규칙 기준으로 갱신.
- 앱 `test/photo_gate_test.dart` — 순번 판정·다음 순번 안내 + 작성 화면 문구 노출.

## 6. 회피 시나리오 점검

| 시도 | 결과 |
| --- | --- |
| 글을 지우고 다시 쓴다 | 누적 수는 줄지 않음 → 순번 그대로 |
| 인증 필요한 새 펫 + 오래된 펫을 함께 연결 | 하나라도 인증 순번이면 그 글은 인증 필요 |
| 오래된 펫을 촬영해 새 펫 게이트를 통과 | 가능(2026-07-20 완화 유지). 새 펫의 누적은 올라가므로 4·10번째 게이트는 그대로 온다 |
| 검증 없는 카테고리(자유·소식)로 누적을 쌓는다 | 가산 대상이 아님(검증 카테고리만 카운트) |
