# 0033 — 시설 데이터가 6주간 얼어 있던 문제: 적재 파이프라인 복구와 제보 경로

> 상태: **조치 완료·운영 적용**. 작성 2026-08-11.
> 대상: pmdb(DB·Edge Function·스크립트) + pmdart(앱).
> 관련 PR: pmdb #183~#191 · pmdart #305~#310 (전부 머지).
> 관련 문서: **0021(시설 지도 — §4 적재 설계)**, 0022(시설 후기), 0025(업체 인증),
> 0028(오프라인 제휴 QR).
>
> 발단은 코드가 아니었다. 사용자가 **직접 매장을 방문해서** 한 곳이 이전한 걸 알았고,
> "전국적으로 비일비재할 텐데 어떻게 대처하나"는 질문에서 시작해 파고들었더니
> **시설 데이터가 2026-06-27 이후 갱신될 길이 아예 없었다.** 설계(0021 §4)는 있는데
> 구현이 통째로 비어 있었고, 설령 구현했어도 붙지 않는 상태였다.

---

## 1. 배경 — 무엇이 얼어 있었나

`public.facilities` 24,550건은 2026-06-27 에 **QGIS 수작업으로 1회 적재**된 것이 전부였다.
0021 §4 가 설계한 `sync-facilities` Edge Function 과 `upsert_facilities` RPC 는 **존재하지
않았고**, cron 도 없었다. 그래서 그날 이후 폐업·이전·신규 개업이 하나도 반영되지 않았다.

문제는 "아직 안 만들었다"가 아니라 **만들어도 안 붙는 상태였다**는 것이다. 진단 결과 네 겹이었다.

### 1.1 `ext_id` 가 재현 불가능한 값이었다

upsert 키는 `(source, ext_id)` 인데, 초기 적재의 `ext_id` 는 `5568e1c5381a3dbd8e0b` 같은
**20자리 16진 생성 해시**였다. 사업장명·주소·관리번호를 md5/sha1/sha256 여러 조합으로 맞춰
봤지만 일치하는 규칙이 없었다. 원본에서 그 값을 다시 만들 수 없으니 `on conflict` 가 성립하지
않는다 → **갱신본을 받아도 기존 행에 못 붙고 새 행이 통째로 쌓인다.**

### 1.2 폐업을 걸러서 적재했다

초기 적재가 `영업/정상`만 골라 넣어 DB에 폐업 행이 **0건**이었다. 이 상태에서 같은 방식으로
재적재하면, 문 닫은 가게의 기존 행은 새 추출본에 **없으니까 갱신 대상이 아니어서**
`is_open=true` 로 영영 남는다. 지도는 `facilities_within` 의 `is_open` 으로만 거르므로
그대로 노출된다.

### 1.3 xlsx 를 거치며 데이터가 깨져 있었다

원본 CSV(CP949)를 Excel 로 열어 xlsx 로 저장한 뒤 적재한 흔적이 데이터에 남아 있었다.

| 증상 | 원인 |
|---|---|
| `#NAME?` 라는 이름의 미용실 | 상호 `+더개꿀` 을 Excel 이 수식으로 해석 |
| `전북특별자���도`, `치�� 애견유치원`, `영업/���상` (17건) | CP949→UTF-8 변환 손상 |
| 관리번호 `3.0000000492023002E+17` | 18자리 정수를 부동소수점으로 변환 → 끝자리 유실 |

세 번째가 특히 치명적이다. 관리번호를 키로 쓰려면 xlsx 를 **거치면 안 된다**.

### 1.4 공공데이터 자체가 몇 주 늦다

2026-08-10 에 data.go.kr 에서 새로 받았는데 최신 `최종수정시점` 이 **2026-06-23** 이었다.
즉 받은 파일이 이미 있던 것과 같은 스냅샷이었다. LOCALDATA 는 실시간이 아니라 주기 배포본이다.
여기에 사장님이 시청에 신고하는 지연이 더해져 **폐업·이전 반영의 하한이 2~3개월**이다.

---

## 2. `ext_id` 를 LOCALDATA 관리번호로 정착 (pmdb #189)

CSV 원본(CP949, 관리번호가 텍스트로 온전)과 기존 24,550행을 대조해 `ext_id` 를 교체했다.

**매칭 규칙**

| 순서 | 기준 | 건수 |
|---|---|---|
| ① | `(category, 사업장명, 도로명주소)` | 24,492 |
| ② | 도로명주소가 없으면 지번주소 폴백 | 44 |
| ③ | 깨진 문자 구간을 와일드카드로 둔 정규식 | 14 |

**24,550건 전부 매칭, 관리번호 중복 0건.** 후보가 여럿이면 `영업/정상 → 휴업 → 취소/말소 → 폐업`
순으로 골랐다.

③이 필요했던 이유: 손상된 17건은 문자열이 깨져 있어 정확 일치로는 못 찾는다. `U+FFFD` **연속
구간**을 `.{1,3}` 으로 치환해 매칭했다(깨진 글자 하나가 `���` 처럼 여러 개로 늘어나 문자 수가
안 맞기 때문). 같은 작업에서 손상 문자를 원본 값으로 교정했다.

옛 값은 `facilities.legacy_ext_id` 에 보존했다. 되돌리려면:

```sql
update public.facilities set ext_id = legacy_ext_id where legacy_ext_id is not null;
```

⚠️ 데이터 교체 자체는 외부 CSV 대조라 1회성으로 운영에 직접 적용했다. 마이그레이션에는
되돌림용 컬럼만 담겨 있다.

---

## 3. 좌표 — DB 변환으로 충분하다 (초기 판단 정정)

작업 중 "기존 핀이 QGIS 산출물이니 `ST_Transform` 을 쓰면 어긋난다, 약 16 m 차이"라고 판단했다가
**틀린 것으로 확인해 뒤집었다.**

그 16 m 는 PostGIS 기본 변환과 **임의로 지정한 7파라미터** 사이의 차이였지, QGIS 와의 차이가
아니었다. `spatial_ref_sys.proj4text` 에 `towgs84` 가 없어 데이텀 보정이 빠졌다고 오해했는데,
PostGIS 3.x 는 그 컬럼이 아니라 **PROJ 자체 데이터베이스**를 쓴다.

실측: 원본 5174 좌표 3,000건을 `ST_Transform` 한 결과와 저장된 QGIS 좌표를 비교했다.

```
  n   | min_m | avg_m | p95_m | max_m
 3000 |  0.00 |  0.00 |  0.00 |  0.00
```

**오차 0.00 m.** 따라서 `upsert_facilities` 가 DB 안에서 변환한다. QGIS 재실행은 필요 없다.

---

## 4. 적재 파이프라인 (pmdb #190)

### 4.1 `public.upsert_facilities(jsonb)` — service_role 전용

0021 §4 설계에서 두 가지를 바꿨다.

| | 0021 | 실제 | 이유 |
|---|---|---|---|
| 스키마 | `app.` | **`public.`** | PostgREST 가 `app` 을 노출하지 않아 `.rpc()` 로 못 부른다. 권한은 GRANT 로 막음 |
| 좌표 | (미정) | **DB `ST_Transform`** | §3 참조 |

**정책**

- 업주가 고친 행(`owner_updated_at`)은 **간판명·전화를 보존**하고 주소·업종·영업상태만 덮는다.
  주소·업종·영업상태는 인허가 데이터 영역이기 때문이다(20260715130000 주석).
- 좌표 없는 **신규** 행은 넣지 않는다(지도에 못 찍는다). **기존** 행은 좌표가 안 와도 상태는 갱신.
- 같은 배치에 같은 `ext_id` 가 중복되면 하나만 남긴다(`ON CONFLICT` 는 배치 내 중복을 못 견딘다).

### 4.2 `sync-facilities` Edge Function

`x-sync-secret` 헤더를 검증하고 배치(≤1000행)를 RPC 로 넘긴다. **0021 은 이 함수가 CSV 를 직접
내려받는 그림이었으나 그렇게 하지 않았다** — 배포본이 카테고리당 3~4 MB CP949 CSV 이고 4종
합쳐 6만 행이라, 엣지 런타임에서 디코딩·파싱하면 메모리·시간 한계에 걸리고 실패해도 어디까지
처리됐는지 알 수 없다. 파싱은 호출자가 하고 함수는 검증된 배치만 받는다.

시크릿은 Supabase 함수 시크릿 `SYNC_SECRET`, 로컬 값은 `scripts/sync.env`(gitignore).

### 4.3 `scripts/sync_facilities.py`

CSV 4종을 파싱해 500행씩 전송한다.

```bash
set -a && . scripts/sync.env && set +a
./scripts/sync_facilities.py <CSV폴더> --dry-run   # 상태 분포 먼저 확인
./scripts/sync_facilities.py <CSV폴더>
```

macOS 가 압축 해제 경로에 따라 파일명을 **NFD** 로 남겨 카테고리 매칭이 조용히 실패했다
(같은 폴더에서 3개는 NFD, 따로 받은 1개는 NFC였다) → `unicodedata.normalize("NFC", ...)`.

필터링된 파일(폐업·휴업이 없는)을 감지하면 경고한다.

### 4.4 1회 실행 결과 (2026-08-11)

```
합계: {'updated': 24550, 'inserted': 19303, 'skipped': 2072}
```

| | 적재 전 | 적재 후 |
|---|---|---|
| 총 행 | 24,550 | 43,853 |
| 영업중 | 24,550 | 24,568 |
| 폐업 등 | 0 | **19,285** |

- **기존 24,550건 전부 갱신**(`legacy_ext_id` 보유분 전수 확인) — 중복 행 0. ext_id 정착이 의도대로 동작.
- `skipped: 2,072` 는 좌표 없는 신규 행. 지도에 찍을 수 없어 의도적으로 제외.
- 폐업 19,285건이 사용자에게 새지 않는지 실측 확인: 검색·지도 반경 모두 0건 노출,
  폐업 시설에 걸린 QR 0건, 폐업 시설에 달린 후기 0건.

---

## 5. 지연을 메우는 제보 경로 (pmdb #191 · pmdart #310)

§1.4 대로 자동 경로만으로는 2~3개월이 하한이다. **가게 앞에 선 사용자가 가장 빠른 탐지기**인데
제보할 길이 없었다.

### 5.1 핵심 — 수동 판정이 재적재를 이겨내야 한다

제보를 받아 `is_open=false` 로 내려도, 다음 재적재는 LOCALDATA 를 믿고 다시 `true` 로 되돌린다
(그쪽은 아직 '영업/정상'이니까). **이 한 가지를 빠뜨리면 제보 기능이 조용히 무력해진다.**

`facilities.reported_closed_at` 을 두고 `upsert_facilities` 가 그 행의 `is_open` 을 올리지
않게 했다 — `owner_updated_at` 이 간판명·전화를 지키는 것과 같은 방식이다. 원천이 따라잡으면
(폐업으로 바뀌면) 표시를 자동으로 지운다. 근거가 공공데이터로 넘어가므로 수동 판정을 유지할
이유가 없기 때문이다.

### 5.2 나머지

- `reports.target_type` 에 `'facility'`, 사유에 `폐업했어요`·`이사갔어요`·`정보가 달라요`
- `admin_mark_facility_closed(uuid, boolean)` — `is_admin` 게이트. 해제 시 임의로 `true` 를
  넣지 않고 원천 `biz_status` 로 되돌린다
- `admin_get_report_target` 에 facility 분기(관리자가 무엇을 신고받았는지 본다)
- 앱: 시설 상세에 `정보가 달라요 · 폐업/이전 제보`(기존 신고 시트 재사용), 관리자 신고 상세에 판정 버튼

앱에서 주의한 두 가지: 시설 상세는 **무한 너비**라 `Align` 이 터진다(`Row(mainAxisSize.min)` 사용),
네이버 카페는 마커 id 가 가짜라 그대로 제보하면 관리자가 대상을 못 찾는다(후기와 같은 기준인
승격된 실제 id 를 쓰고, 못 구했으면 링크를 노출하지 않음).

### 5.3 검증 (운영 DB 롤백 트랜잭션)

| 단계 | 결과 |
|---|---|
| ① 관리자 폐업 판정 | `is_open=f`, 수동표시 `t` |
| ② 재적재가 `영업/정상` 으로 되돌리려 시도 | **`is_open=f` 유지** |
| ③ 원천이 폐업으로 따라잡음 | `is_open=f`, 수동표시 **해제** |
| ④ 일반 사용자 제보 INSERT(`authenticated` 롤 + JWT) | RLS 통과, `status=submitted` |
| ⑤ 관리자 대상 조회 | `kind=facility`, `name` 정상 |

---

## 6. 함께 처리한 감사 후속 (pmdb #183~#188 · pmdart #305~#309)

같은 기간에 별도 감사 지적(사소 항목 배치)을 처리했다. **모든 지적이 타당하지는 않았고, 지적에
없던 것이 하나 나왔다.**

| 항목 | 판정 | 조치 |
|---|---|---|
| 로그아웃 직후 refresh grace 로 **세션 부활** | **지적에 없던 실재 결함** | pmdb #184 |
| 채팅 미읽음 보정이 나간 방·차단 방까지 셈 | 실재(잠복) | pmdb #187 |
| 신원 등록 프레임이 참조 없이 스토리지에 잔존 | 실재(고아 4건) | pmdb #186 #188 |
| enroll 프레임 개수 상한 없음 | 실재 | pmdb #183 |
| 커뮤니티 스피너 고착(silent 갱신 실패 경로) | 실재 | pmdart #309 |
| `push.releaseToken` 거짓 경보 | 실재(로그 등급 강등) | pmdart #309 |
| 채팅 실시간 재구독 세대 가드 누락 | 실재 | pmdart #308 |
| FK 인덱스 누락 16건 | **부분 인정 — 3곳만** | pmdb #185 |
| AI 장애를 HTTP 200 으로 반환 | **고치지 않음** | 아래 |
| 8/1~8/3 unread 배지 고착 | **없음** | 아래 |
| 로그아웃 self-rotation revoke 조용한 실패 | **지적이 틀림** | 아래 |

`rt_revoke_family` 는 family 단위 회수이고 회전은 옛 행을 지우지 않고 `family_id` 를 남기므로,
낡은 토큰으로 불러도 새 토큰까지 회수된다(운영 DB 롤백으로 확인). "조용한 실패"는 없었다.

FK 인덱스는 16곳 전부 달지 않았다 — 가장 큰 테이블이 111행이라 이 규모에서는 순차 스캔이 더
싸고 인덱스는 쓰기마다 비용이 붙는다. 기준을 행 수가 아니라 **부모 삭제가 구조적으로 반복되는가**
로 잡아, `chat_messages` 를 매일 대량 삭제하는 크론이 `ON DELETE SET NULL` 로 두 테이블을 훑는
자리 등 3곳만 달았다.

**`rt_rotate` 세션 부활(#184)** 은 지적에 없던 것으로, 다른 항목을 확인하다 발견했다.
`replaced_by is null` 이면 로그아웃으로 죽은 것이라는 전제로 grace 를 막고 있었는데,
**로그아웃 직전에 회전이 한 번 일어나면** 그 토큰은 `replaced_by` 를 갖게 되어 방어를 통과한다.
로그아웃 호출 시 access 만료가 가까우면 그 호출 자체가 회전을 유발하므로 드문 순서가 아니다.
후속 토큰의 상태로 "회전으로 죽었나 / 회수로 죽었나"를 가르도록 고쳤다(t24).

**AI 장애 200 은 의도적으로 두었다.** 클라이언트가 `res.data['reason']` 을 읽어 사용자 문구로
매핑하는데, 503 으로 바꾸면 `functions.invoke` 가 예외를 던져 그 매핑에 도달하지 못하고
"알 수 없는 오류"로 퇴보한다. 그리고 알람은 이미 `alertAdmins` → `ops_alarms` 로 나간다.
200 이 왜곡하는 건 플랫폼 HTTP 대시보드뿐이다. 고치려면 서버(503 + 본문 `reason` 유지)와
클라이언트(`FunctionException.details` 파싱)를 함께 바꿔야 한다.

**배지 고착은 없었다.** 지적된 것은 **채팅** 카운터(`unread_chat_count`)인데 처음에 **알림**
카운터를 실측하고 닫을 뻔했다 — 카운터가 둘이라는 걸 놓쳤다. 채팅 쪽을 직접 재니
`user_blocks` 역대 0건, `unread_chat_count > 0` 인 사용자 0명이라 소급 보정 대상이 없었다.
다만 원인 코드는 잠복해 있어 #187 로 닫았다.

---

## 7. 운영 절차

### 7.1 재적재

1. data.go.kr 에서 **CSV** 4종을 받는다(동물병원·동물미용업·동물위탁관리업·**동물판매업**).
   0021 설계에 판매업이 빠져 있어 놓치기 쉽다. 장묘업은 DB에 없다.
2. **받자마자 `최종수정시점` 최댓값을 확인한다.** 몇 주 전 스냅샷이면 바뀌는 게 없다.
3. `--dry-run` 으로 상태 분포를 본다. **폐업·휴업이 0건이면 필터링된 파일이다 — 쓰면 안 된다.**
4. 실행 후 `is_open` 분포와 `legacy_ext_id` 보유분 갱신 여부를 확인한다.

### 7.2 절대 하지 말 것

- **CSV 를 Excel 로 열지 말 것** — §1.3.
- **폐업·휴업을 걸러서 보내지 말 것** — §1.2. 걸러 보내면 기존 행이 안 내려간다.
- **주소를 수기로 고치지 말 것** — 주소는 인허가 데이터 영역이라 보존 장치가 없고 재적재가
  항상 덮는다. 고쳐 두면 조용히 되돌아간다.

---

## 8. 남은 것

- **적재 자체는 여전히 수동.** 함수가 CSV 를 직접 받지 않는 구조라 파일을 받아 스크립트를 돌리는 사람이 필요하다.
  LOCALDATA 개방 API 키를 확보하면 함수가 직접 가져오도록 바꿀 수 있다. 잊지 않게 하는 장치는 둘 있다
  (pmdb #198, 마이그레이션 `20260907090000_facility_resync_reminder_and_frozen_watch`, pgTAP t26):
  - pg_cron `facility-resync-reminder`(매월 1일 00:00 UTC) — `ops_alarm_fire('facility_resync_due', 1440, …)` 로
    §7.1 절차를 상기시키는 알람만 올린다.
  - 트리거 `trg_facilities_frozen_addr_alert`(facilities AFTER UPDATE, `owner_updated_at` 이 세워진 행의 `address` 가
    바뀔 때) — 아래 "임시 개명 1건" 의 주소 덮어쓰기를 사람이 눈으로 찾는 대신 `facility_frozen_addr:<id>` 알람
    (행별 1일 쿨다운)으로 받는다. 알림 실패는 warning 으로 삼켜 적재 배치를 막지 않는다.
  - 이 마이그레이션은 운영에 적용됐지만 `supabase_migrations.schema_migrations` 에 기록되지 않았었다 —
    2026-09-12 이력 repair 로 등록(같은 날 08-10 분 6건 미기록·3건 버전 불일치도 함께 정정).
- **좌표 없는 영업중 업소 2,072건**이 계속 제외된다. 필요해지면 주소 지오코딩으로 채우는 별도 작업.
- **`facility_all_categories` 는 `is_open` 을 보지 않는다.** 미용 인허가는 폐업했는데 병원
  인허가는 살아 있는 업소가 미용 배지까지 달 수 있다. 화면상 배지 하나 차이라 미조치.
- **임시 개명 1건.** 이전한 매장의 옛 자리에 다른 미용실이 들어왔는데 공공데이터에 아직 없어,
  사용자 지시로 옛 행의 `name` 을 새 가게 이름으로 바꾸고 `owner_updated_at` 을 세워 뒀다.
  **이름은 얼렸지만 주소는 못 얼렸다** — 원 매장의 이전이 LOCALDATA 에 반영되는 순간
  "새 가게 이름 + 옮겨간 주소" 라는 어긋난 행이 된다. **재적재 때마다 이 행을 확인할 것.**
- 외부 데이터(네이버 지역검색) 대조는 보류. 2.4만 건 대조는 쿼터·오탐 처리 비용이 크고,
  현재 후기 규모(전체 5건)에서는 이르다.
