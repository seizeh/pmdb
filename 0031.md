# 0031 — 사각지대 감사: 실사용으로 드러나지 않는 결함 전수

> ⚠️ **후속 조치는 [0032.md](0032.md) 참고 (2026-08-04).** 아래 "❌" 로 남은 항목 중
> §3.1 §3.2 §5.1 §5.2 §5.5 §5.6 §3.6 등 대부분이 처리됐고, 이 문서의 상태 표는
> **작성 시점 그대로 두었다**(감사 기록이므로). 무엇이 남았는지는 0032 §9 가 정본이다.
>
> 0032 는 또한 이 문서의 **분류가 놓친 유형**을 하나 추가한다 — "방어가 없는 곳" 이
> 아니라 **"방어가 있는데 일부 경로만 적용된 곳"**. 실제로 더 위험했다(0032 §0).

> 상태: **감사 기록 + 일부 조치 완료**. 작성 2026-08-03 (같은 날 실측 반영 1차 개정).
> 대상: pmdb(마이그레이션·Edge Functions·CI) + pmdart(앱·CI) + GitHub 저장소 설정.
> 관련 PR: pmdb #151 #152 · pmdart #254 #255 (작성 시점 전부 열림, CI 통과).
>
> 다른 00xx 문서와 성격이 다르다. 저 문서들은 "무엇을 만들었나"의 설계 기록이고,
> 이건 **"무엇이 이미 잘못돼 있는데 아무도 모르고 있었나"** 의 목록이다. 그래서
> 조치한 것보다 **조치하지 않은 것이 훨씬 많고**, 그 목록이 이 문서의 본체다.

## 0. 이 감사의 기준 — '사각지대'의 정의

앱을 아무리 많이 써 봐도 드러나지 않는 결함만 모았다. 판정 기준은 하나다:

> **정상 경로를 정상적으로 밟는 사람에게 증상이 보이는가?**
> 보이면 이 문서 대상이 아니다(버그 트래커의 몫이다).

그래서 아래 것들은 전부 제외했다 — 레이아웃 깨짐, 오타, 느린 화면, 실패하는 버튼.
남은 건 네 가지 유형이다.

| 유형 | 왜 안 보이나 | 예 |
| --- | --- | --- |
| **공격자만 밟는 경로** | 정상 사용자는 그 요청을 보내지 않는다 | OTP 무제한 대입(§2.1) |
| **비용·자원으로만 드러남** | 기능은 정상 동작한다. 청구서에만 나타난다 | SMS 남용(§2.2), 유료 API(§3.2) |
| **조용히 죽는 경로** | 실패해도 화면이 아무 말을 하지 않는다 | realtime 재구독(§5.1), 배포 게이팅(§2.3) |
| **규모가 오기 전엔 잠복** | 지금 데이터가 적어서 안 보인다 | 1000행 절단(§5.2), 검색 인덱스(§5.3) |
| **도달 불가에 의존** | 지금은 그 코드에 닿지 않아 무해하다 | `clientIp()` XFF 폴백(§3.7) |

마지막 유형이 이 감사에서 가장 늦게 눈에 들어왔다. **버그가 아니라 "지금은 안 밟는
경로"** 라서 어떤 테스트로도 안 잡히고, 코드를 읽어도 "이건 도달 안 하니까 괜찮다"로
넘어가게 된다. 무해함의 근거가 코드 밖(인프라 구성)에 있으면 그 근거가 바뀌는 순간
아무 신호 없이 결함이 된다.

## 1. 전체 목록과 상태

| # | 항목 | 저장소 | 위험 | 상태 |
| --- | --- | --- | --- | --- |
| 1 | 전화 OTP 대입 횟수 제한 없음 → 계정 인수 | pmdb | 높음 | ✅ #151 |
| 2 | 문자 발송 총량 상한 없음 → 비용·문자 폭탄 | pmdb | 높음 | ✅ #151 |
| 3 | 배포가 테스트 통과와 무관하게 나감 | pmdb·pmdart | 높음 | ✅ #152 #254 |
| 3-1 | 대입 제한 429의 사용자 문구 부재 | pmdart | 낮음 | ✅ #255 |
| 4 | Edge Function 6종이 정지·세션회수 게이트를 통과 | pmdb | 중간 | ❌ §3.1 |
| 5 | 유료 외부 API 호출 함수에 레이트리밋 없음 | pmdb | 중간 | ❌ §3.2 |
| 6 | DEMO_PHONES/DEMO_OTP 백도어 상주 | pmdb | 중간 | ❌ §3.3 |
| 7 | 계정 열거(전화번호 가입 여부 대조) | pmdb | 낮음 | ❌ §3.4 |
| 8 | 레이트리밋 fail-open이 무알림 | pmdb | 낮음 | ❌ §3.5 |
| 9 | Edge Function TypeScript 검사가 CI에 없음 | pmdb | 낮음 | ❌ §3.6 |
| 9-1 | `clientIp()`가 DB 경로와 달리 XFF 폴백 잔존 | pmdb | 낮음 | ✅ #151 (§3.7) |
| 10 | Dependabot 알림 비활성 | 양쪽 | 중간 | ❌ §4.1 |
| 11 | Secret scanning 경보 3건 방치 | pmdart | 중간 | ❌ §4.2 |
| 12 | Actions SHA 미고정 | 양쪽 | 낮음 | ❌ §4.3 |
| 13 | realtime 구독 실패가 영구화 | pmdart | 중간 | ❌ §5.1 |
| 14 | 목록 쿼리가 1000행에서 소리 없이 잘림 | pmdart | 중간 | ❌ §5.2 |
| 15 | 검색이 인덱스 못 쓰는 선행 와일드카드 | pmdart·pmdb | 중간 | ❌ §5.3 |
| 16 | 접근성 전무(Semantics 0곳) | pmdart | 중간 | ❌ §5.4 |
| 17 | Android auto backup 미차단 | pmdart | 중간 | ❌ §5.5 |
| 18 | release 서명이 debug로 조용히 폴백 | pmdart | 중간 | ❌ §5.6 |
| 19 | iOS `aps-environment: development` | pmdart | 중간 | ❌ §5.7 |
| 20 | 웹 푸시 SW의 Firebase SDK 버전 수동 고정 | pmdart | 낮음 | ❌ §5.8 |
| 21 | 웹 응답에 CSP 외 보안 헤더 없음 | pmdart | 낮음 | ❌ §5.9 |
| 22 | 오류를 모으는데 알려 주는 장치가 없음 | 양쪽 | 중간 | ❌ §6.1 |
| 23 | 커버리지 19.1%, 41개 파일 0% | pmdart | 낮음 | ❌ §6.2 |

---

# 2. 조치한 것

## 2.1 전화 OTP 대입 횟수 제한 없음 (#151)

**대상**: `supabase/functions/verify-phone-code/index.ts`

### 무엇이 문제였나

틀린 코드를 넣어도 세 가지 중 아무것도 일어나지 않았다.

- `is_used`가 바뀌지 않는다 → 같은 코드에 계속 찍어 볼 수 있다
- 카운터가 없다 → `phone_verifications` 테이블에 `attempts` 컬럼 자체가 없다
  (`supabase/schema/baseline.sql:2538`)
- `rateLimited()`를 부르지 않는다 → 호출 횟수도 세지 않는다

6자리 = 100만 조합, 코드 수명 5분, 재발송은 60초당 1회. 5분 안에 100만을 다 훑을
수는 없지만 **훑을 필요가 없다** — 60초마다 새 코드를 받아 가며 계속 찍으면 되고,
확률은 시간이 해결해 준다.

### 왜 심각한가 — 경로의 끝

```
send-phone-code(purpose='password_reset')   ← 피해자에게 문자 1통(무시되기 쉽다)
  → verify-phone-code 무제한 대입            ← 여기가 뚫려 있었다
  → reset-password                           ← 계정 인수
```

`reset-password`는 "30분 내 인증이 완료된 번호"만 통과시킨다. 게이트로 보이지만,
**그 인증 자체를 대입으로 만들 수 있으면 게이트가 아니다.**

### 왜 아무도 몰랐나

실패한 대입은 어디에도 기록되지 않는다. `auth_logs`는 로그인 성공만 남기고,
`client_errors`는 앱 쪽 오류만 받는다. 관리자 화면에도, 실사용 관찰에도 흔적이 없다.
**성공했을 때만 흔적이 남는데, 성공하면 그건 이미 정상 로그인으로 보인다.**

### 왜 이런 구멍이 생겼나

방어 도구는 이미 있었다. `_shared/auth.ts`의 `rateLimited()`를 아래가 쓰고 있었다:

`login` · `refresh` · `signup-lite` · `invite-guardian` · `apply-business` · `check-business-no`

**전화 OTP 경로만 빠져 있었다.** 특히 아이러니한 건 `signup-lite`(간이 후기)는
제대로 막혀 있었다는 점이다 — 나중에 만든 경로가 더 안전했다. 원래 경로는
"전화 인증은 SMS를 받아야 하니까 그 자체가 게이트"라는 암묵적 전제 위에 있었는데,
그 전제는 **발송**에만 성립하고 **검증**에는 성립하지 않는다.

### 조치

`signup-lite`와 같은 모양·같은 값으로 맞췄다(전화번호 + IP 두 축).

| 버킷 | 값 | 성격 |
| --- | --- | --- |
| `otpverify:phone:<phone>:<purpose>` | 10회 / 600초 | 대입 대상 그 자체라 **스푸핑 불가** |
| `otpverify:ip:<ip>` | 30회 / 600초 | **이것도 스푸핑 불가** — §3.7 참고 |

코드 조회 **전에** 소모한다 — 제한에 걸린 요청이 DB를 읽지 않게.

> **정정(2026-08-03)**: 이 문서의 초판은 IP 버킷을 "보조·스푸핑 가능"으로 적었다.
> `clientIp()`의 주석을 그대로 옮긴 것인데, **이 배포에서는 틀렸다.** 실측으로
> `cf-connecting-ip`는 위조 시 Cloudflare 엣지가 요청 자체를 거부하고(에러 1000),
> 이 배포에서는 그 헤더가 항상 존재한다. 두 버킷 모두 진짜 방어선이다 — §3.7.

산수: 10분당 10회면 100만 조합의 절반(50만)을 훑는 데만 약 **347일**. 반대쪽으로는
오타 몇 번에 막히지 않는다(재발송 자체가 60초당 1회라 정상 사용자가 10분에 10회를
쓸 일이 없다).

### 남긴 것 — 왜 코드를 즉시 폐기하지 않았나

"N회 실패하면 그 코드를 `is_used=true`로 태워 버린다"도 표준 관행이지만 **안 넣었다.**
공격자가 피해자의 번호로 아무 코드나 5번 넣으면 피해자의 정상 가입이 매번 깨진다 —
**대입 방어가 그대로 DoS 도구가 된다.** 레이트리밋만으로 대입은 이미 불가능하므로
추가 이득이 없다.

### 간이 회원가입(0029)에는 적용되지 않는다 — 이미 막혀 있어서

간이 후기 경로는 `verify-phone-code`를 **거치지 않는다**. `signup-lite`가 검증·계정
확보·토큰 발급을 한 번에 한다(코드 소지와 세션 발급이 벌어지면 남의 인증을 가로챌
창이 생기므로 — 0029의 의도된 설계).

앱 호출부도 그렇다: `lib/widgets/lite_review_auth_sheet.dart:110`이 `signUpLite()`를
직접 부르고 `verifyCode()`는 부르지 않는다.

따라서 새 `otpverify:*` 버킷은 간이 경로를 타지 않는다. 대신 `signup-lite`에
**이미 동일한 방어가 있다** — `lite:phone` 10/600 + `lite:ip` 30/600. 이번에 넣은
값을 여기에 맞춘 것이라 두 경로의 방어 수준이 같다.

앱 문구도 이미 있었다(`LiteSignupResult.message`의 `'rate_limited'`). `PhoneVerifyResult`만
빠져 있어 pmdart#255로 채웠다(§2.4).

---

## 2.2 문자 발송 총량 상한 없음 (#151)

**대상**: `supabase/functions/send-phone-code/index.ts`

### 무엇이 문제였나

제한이 `phone+purpose 60초 1회` 하나뿐이었다. 그건 **한 사람이 재발송을 연타하는
것**을 막는 UX 쿨다운이지, 발송 **총량**을 막는 장치가 아니다.

번호를 바꿔 가며 부르면 상한이 없다. 결과가 둘이다:

1. **실비용** — 문자 한 통마다 Solapi 요금이 나간다
2. **제3자 피해** — 우리 발신번호로 남의 폰에 문자가 꽂힌다. 즉 우리 서비스가
   문자 폭탄의 도구가 된다

특히 `purpose='review'`(간이 후기, 0029)는 **계정 존재 검사를 하지 않는다.**
그게 설계 의도다 — 정식 회원도 간이 경로로 후기를 써야 하므로 `phone_taken`에
막히면 안 된다. 그런데 그 결과 **가입 여부와 무관하게 아무 번호로나** 보낼 수 있는
통로가 열려 있었다. 세 목적 중 가장 남용 여지가 컸다.

### verify_jwt는 방어가 아니다

`config.toml`은 이 함수에 `verify_jwt = true`를 걸어 두었고, `docs/supabase-api.md`
§6(412행)은 이를 "얇은 남용 게이트"로 기록하고 있다. **얇다기보다 없다** — publishable
키는 앱 번들에 실려 있고 저장소도 공개다. 키를 꺼내는 데 리버싱이 필요 없다.

(부수적으로 이 함수의 헤더 주석은 `verify_jwt=false`라고 적고 있어 `config.toml`과
어긋나 있었다. `docs/supabase-api.md` §6(412행)이 "주석 갱신 필요"로 이미 지목한 항목이다.
#151에서 그 문장을 지우고 "왜 방어가 아닌지"로 대체했다. **`verify-phone-code`의
같은 주석은 아직 남아 있다** — §7.3 참고.)

### 왜 아무도 몰랐나

기능이 **정상 동작**하기 때문이다. 남용이 일어나는 동안에도 가입은 되고 문자는 가고
화면은 멀쩡하다. 드러나는 경로가 **월말 Solapi 청구서 하나뿐**이었다.

### 조치 — 세 층

| 층 | 버킷 | 값 | 막는 것 |
| --- | --- | --- | --- |
| ① | 기존 `phone_verifications` 조회 | 60초 1회 | 개인의 재발송 연타 |
| ② | `sms:ip:<ip>` | 20 / 3600초 | 한 출처가 번호를 갈아타며 쏟아내는 것 |
| ③ | `sms:global` | 200 / 3600초 | ②를 우회한 분산 시도 — **비용 상한** |

**②를 계정 존재 검증보다 앞에 뒀다.** 뒤에 두면 `phone_taken`(409) /
`user_not_found`(404) 응답으로 번호 가입 여부를 훑는 호출이 아무 대가 없이 지나간다.
그 열거가 문자 발송의 앞단이다(§3.4와 연결).

**②는 우회 가능한 보조선이 아니다.** `cf-connecting-ip`는 위조하면 Cloudflare 엣지가
요청 자체를 거부하고, 이 배포에서는 그 헤더가 항상 붙는다(§3.7). 즉 ②만으로도
한 출처의 발송량은 실제로 묶인다 — ③은 그 위에 얹는 총량 보험이지 ②의 대체물이 아니다.

**③은 걸리면 정상 가입·비번재설정까지 함께 막는다.** 그래서 조용히 막지 않고
`alertAdmins()`로 관리자에게 인앱+푸시 알림을 띄운다. `edge_alert`의 30분 스로틀이
걸려 있어 폭주해도 알림은 폭주하지 않는다.

**창을 1시간으로 잡은 이유**: 잘못 걸려도 최대 1시간 뒤 스스로 풀린다. 일 단위로
잡으면 오탐 한 번에 하루치 가입이 통째로 막힌다.

데모 번호는 문자를 보내지 않으므로 ②③ 어느 층도 소모하지 않는다.

### ⚠️ 열린 판단 — IP 상한이 너무 빡빡할 수 있다

`sms:ip` 20/시간은 **기존 버킷들보다 유독 뻑뻑하다**:

| 버킷 | 비율 |
| --- | --- |
| `login:ip` (기존) | 20 / **분** |
| `refresh:ip` (기존) | 120 / **분** |
| `lite:ip` (기존) | 30 / 10분 = 3/분 |
| `sms:ip` (신규) | 20 / **시간** = 0.33/분 |

문제는 **국내 이동통신 3사의 CGNAT**이다. 수백~수천 가입자가 하나의 공인 IP를
공유하므로, 매장 QR 후기처럼 **불특정 다수가 짧은 시간에 몰리는** 흐름에서 같은
NAT 뒤의 21번째 사람이 정상 후기를 쓰려다 막힐 수 있다.

증상이 "정상 사용자가 가입/후기를 못 씀"이라 나쁜 쪽이다. 베타 규모에서는 안 닿지만
출시 후 매장 QR이 돌기 시작하면 재평가가 필요하다.

**이 우려는 IP 신뢰도와 무관하다.** CGNAT은 **정당한 공유**라 스푸핑 여부와 축이
다르다 — 헤더가 위조 불가하다는 사실(§3.7)은 "20이 맞는 값인가"에 답하지 않는다.
오히려 IP를 믿을 수 있다는 게 확인됐으므로 **완화 쪽으로 기운다**: 값을 올려도
그 상한이 실제로 지켜지기 때문이다(믿을 수 없는 신호였다면 올리는 순간 무의미해진다).

**완화 선택지**(아직 결정 안 함):

- `IP_MAX`를 20 → 60/시간으로 완화. 공격자 1 IP는 60/시간으로 **확실히** 묶이고
  (우회로가 없다), 총량은 ③(200/시간)이 잡는다. 방어력 손실이 작다.
- `purpose`별로 버킷을 나눠 `review`만 완화(매장 QR 흐름만 풀어 주기).
- 지금 값 유지 + 관측 후 조정. **단 이걸 하려면 §3.5가 선행돼야 한다** — 현재
  ② 발동은 로그에도 안 남아서 "닿았는지"를 알 방법이 없다.

---

## 2.3 배포가 테스트 통과와 무관하게 나감 (#152, pmdart#254)

**대상**: `.github/workflows/deploy-functions.yml`(pmdb) ·
`.github/workflows/deploy-web.yml`(pmdart)

### 무엇이 문제였나

두 배포 워크플로 모두 `on: push: branches:[main]`이라 테스트 워크플로와 **병렬로**
돌았다. `needs:`도 `workflow_run` 게이팅도 없었다.

그리고 **배포가 먼저 끝난다**:

```
pmdart:  CI 3m22s   vs  웹 배포 2m18s     ← 1분 먼저
pmdb:    DB Tests 수 분  vs  함수 배포 ~1분
```

즉 테스트가 빨간불이 되기 전에 이미 프로덕션에 올라가 있다.

### 룰셋이 막아 주지 않는 이유

pmdart의 `main-protection` 룰셋은 `analyze-and-test`를 필수 체크로 걸고 있다.
그런데 그건 **PR 머지 전 커밋** 기준이고, `strict_required_status_checks_policy`가
`false`라 브랜치 최신화도 요구하지 않는다.

```
PR A: main(t0) 기준으로 CI 통과 → 머지
PR B: main(t0) 기준으로 CI 통과 → 머지   ← A를 모른 채
main(t2) = A + B                          ← 이 조합은 아무도 테스트하지 않았다
```

이 머지 커밋의 CI는 새로 돌지만, 배포는 그와 **동시에** 시작해서 먼저 끝난다.
시맨틱 충돌이 그대로 배포된다.

### 왜 아무도 몰랐나

- 배포 실패가 아니라 **배포 성공**이다. Actions는 초록불이다.
- CI가 나중에 빨간불이 돼도 **알림이 없다.** 셀프 머지 환경이라 리뷰어도 없다.
- 배포는 **되돌아가지 않는다.** 깨진 번들이 다음 머지까지 서빙된다.
- pmdb 쪽은 더 나쁘다 — Edge Function은 자동 롤백이 없다.

`deploy-functions.yml`의 헤더는 스스로 존재 이유를 "main = 배포본"이라고 적어 두었다.
그런데 정작 **테스트가 거부한 코드가 배포본이 되는** 구멍은 열려 있었다.

### 조치

```diff
 on:
-  push:
-    branches: [main]
-    paths: [...]
+  workflow_run:
+    workflows: ["DB Tests"]   # pmdart 는 ["CI"]
+    types: [completed]
   workflow_dispatch:
```

`workflow_run`은 성공·실패·취소를 가리지 않고 발화하므로 세 조건을 모두 본다:

```yaml
github.event.workflow_run.conclusion == 'success' &&
github.event.workflow_run.event == 'push' &&
github.event.workflow_run.head_branch == 'main'
```

체크아웃은 `github.event.workflow_run.head_sha`로 **고정**한다. 기본값(main 최신)으로
두면 테스트 이후 들어온 미검증 커밋을 배포하게 된다 — 고치려던 문제가 그대로 돌아온다.

### 대가 둘 (인지하고 수용)

**1. `paths:` 필터를 잃는다** — `workflow_run`에는 없다. `gate` 잡에서 `HEAD^`와
직접 비교해 대체했다.

⚠️ **판정이 애매하면 배포하는 쪽으로 넘어간다(fail-open).** 부모 커밋이 없거나,
diff가 실패하거나, 결과가 비면 무조건 배포다. 불필요한 배포는 1~2분을 버리는 게
전부지만, 필요한 배포를 건너뛰면 main ≠ 배포본이 되고 그게 이 워크플로가 존재하는
이유 그 자체다.

다만 이 대체물은 **머지 커밋 흐름을 전제**한다(`HEAD^` = 이전 main 팁). main에 여러
커밋을 한 번에 직접 푸시하면 앞쪽 커밋의 변경을 놓칠 수 있다. 룰셋이 필수 체크를
요구해 실제로는 그 경로가 거의 없고, 놓치더라도 `workflow_dispatch`가 안전판이다.

**2. 배포가 테스트만큼 늦어진다** — pmdart ~3분, pmdb 수 분.

### ⚠️ 머지 후 확인 필수

`workflow_run` 등록은 **파일이 기본 브랜치에 올라간 뒤에** 유효해진다. 머지 직후
첫 배포가 안 뜰 수 있다 — 안 뜨면 `workflow_dispatch`로 한 번 돌린다.

그리고 **`workflows:` 문자열이 상대 워크플로의 `name:`과 정확히 일치해야 한다.**

| 배포 워크플로 | 참조하는 이름 | 정의 위치 |
| --- | --- | --- |
| pmdb `deploy-functions.yml` | `"DB Tests"` | `db-tests.yml:5` |
| pmdart `deploy-web.yml` | `"CI"` | `ci.yml:3` |

**어긋나면 배포가 조용히 안 뜬다.** 실패도 경고도 없다 — 이 문서가 다루는 바로 그
유형의 결함을 새로 만드는 셈이므로, 어느 쪽 `name:`을 바꿀 일이 생기면 반드시 짝을
같이 고쳐야 한다.

---

## 2.4 대입 제한 429의 사용자 문구 부재 (pmdart#255)

**대상**: `lib/services/phone_auth_service.dart`의 `PhoneVerifyResult.message`

#151이 `verify-phone-code`에 `429 { error: 'rate_limited' }`를 새로 만들었는데,
앱의 매핑에 그 분기가 없어 기본값으로 떨어졌다:

```dart
_ => '인증에 실패했어요',
```

**문구 하나 차이가 아니다.** "실패했어요"는 *틀렸다*는 뜻으로 읽힌다. 사용자는 같은
자리에서 계속 다시 넣는데, 이미 막힌 구간이라 무엇을 넣어도 실패하고 재시도가
창을 더 연장한다. 빠져나올 방법을 화면이 알려주지 않는 상태가 된다.

같은 코드에 대한 문구가 `PhoneCodeResult`("잠시 후 다시 시도해주세요")와
`LiteSignupResult`("시도가 너무 많아요…")에는 **이미 있었다** — 검증 쪽만 빠져 있었다.

매핑 테스트 3건(신규 코드 + 기존 코드 회귀 + 기본값)을 함께 넣었다.

**머지 순서**: pmdart#255를 pmdb#151보다 **먼저**. 반대로 하면 그 사이 제한에 걸린
사용자가 "인증에 실패했어요"만 보고 계속 재시도한다.

---

# 3. 남은 것 — 서버(pmdb)

## 3.1 Edge Function 6종이 정지·세션회수 게이트를 통과한다 [중간]

DB 경로는 확실히 잠겨 있다 — `app.uid()` (`supabase/schema/baseline.sql:1880`):

```sql
select u.id from public.users u
where u.id = ...->>'sub'
  and u.status = 'active'                                    -- 정지 즉시 반영
  and u.token_version = coalesce((...->>'tv')::int, 0)       -- 비번 변경 시 회수
```

**그런데 JWT를 직접 검증하고 service_role로 진행하는 함수들은 서명 + `exp`만 본다.**

| 함수 | 검증 방식 | status | tv |
| --- | --- | --- | --- |
| `verify-location` | 자체 `getUidFromJwt` | ❌ | ❌ |
| `verify-post-photo` | 자체 사본 | ❌ | ❌ |
| `enroll-pet-identity` | 자체 사본 | ❌ | ❌ |
| `resolve-region` | 자체 사본 | ❌ | ❌ |
| `search-petcafe` | 자체 사본 | ❌ | ❌ |
| `sync-dong-centroids` | 자체 사본 | ❌ | ❌ |
| `apply-business` · `check-business-no` | `_shared/verifyAccess` | ❌ | ❌ |
| `invite-guardian` | `verifyAccess` + **직접 status 조회** | ✅ | ❌ |

**결과**: 비밀번호 재설정으로 회수된 토큰과 **정지·차단된 계정**이 access 수명 동안
지역인증·사진인증·펫 신원등록을 계속 할 수 있다. 수명은 refresh 지원 클라 8시간,
**레거시 토큰은 30일**이다.

관리자 화면에서 계정을 정지시켜도 이 함수들에서는 아무 일도 일어나지 않고, 그 사실이
어디에도 표시되지 않는다.

`invite-guardian:50`의 주석 — *"호출자 상태(active) + 펫 owner 확인 — service_role
경유라 직접 검증한다"* — 이 패턴이 정답인데 나머지가 안 따르고 있다.

**권장 조치**: `_shared/verifyAccess()`에 status/tv 확인을 넣고, 자체 사본 6개를
그것으로 통일한다. 사본이 6개나 되는 것 자체가 드리프트 원인이다.

## 3.2 유료 외부 API 호출 함수에 레이트리밋 없음 [중간]

| 함수 | 호출하는 유료 API |
| --- | --- |
| `verify-post-photo` · `enroll-pet-identity` | Google **Gemini** |
| `resolve-region` · `verify-location` | Naver Reverse Geocoding |
| `search-petcafe` | Naver Local Search |

전부 `rateLimited()`를 쓰지 않는다. **로그인 사용자 한 명이 루프로 호출하면 그대로
비용**이다. 앱 UI는 한 번씩만 부르니 실사용 관찰로는 영원히 안 보인다.

§3.1과 합치면 더 나쁘다 — **정지된 계정도** 태울 수 있다.

## 3.3 DEMO_PHONES / DEMO_OTP 백도어 상주 [중간]

`send-phone-code:44`. 시크릿이 설정된 동안 해당 번호는 **고정 OTP + 레이트리밋 면제**다.
심사가 끝나면 해제해야 하는데 **코드에도 CI에도 리마인더가 없다.**

`docs/supabase-api.md` §4 `send-phone-code`(151행)가 "앱 심사 기간에만 켠다"고 적어 두었지만, 문서는 알람이
아니다. App Review 재제출(2026-08-01) 이후 상태를 Supabase 시크릿에서 직접 확인해야
한다.

**권장 조치**: 최소한 활성 상태일 때 `alertAdmins`로 주기적 리마인드, 또는 시크릿에
만료일을 넣고 지나면 자동 비활성.

## 3.4 계정 열거 [낮음]

`send-phone-code`가 목적별로 구분 응답한다:

- `signup` + 기존 번호 → `409 phone_taken`
- `password_reset` + 미가입 번호 → `404 user_not_found`

함수 주석은 *"같은 정보는 어차피 최종 단계에서 노출되므로 계정 열거 위험이 새로
늘지 않음"* 으로 수용했다. **절반만 맞다** — UI를 거치면 한 번에 한 명이지만, API를
직접 부르면 **번호 목록을 대량으로 대조**할 수 있다. 위험의 종류가 아니라 규모가 다르다.

#151의 `sms:ip` 버킷이 앞단에 놓이면서 대량 열거 속도는 IP당 20/시간으로 묶였다.
근본 해결은 아니지만 실용적 상한은 생겼다.

## 3.5 레이트리밋 fail-open이 무알림 [낮음]

`_shared/auth.ts`의 `rateLimited()`는 리미터 자체가 오류를 내면 **`false`(허용)를
반환**한다. 가용성 우선의 의도된 절충이고 문서에도 적혀 있다.

문제는 **그 상태가 조용하다는 것**이다. `rate_limit_hit` RPC가 실패하면
`console.error`만 찍히고, 그동안 모든 레이트리밋이 사라진다. Edge Function 로그를
누가 열어 보지 않으면 방어선이 통째로 내려간 채로 시간이 흐른다.

같은 맥락에서 **버킷 발동 자체도 관측되지 않는다** — #151의 `sms:ip`(②)와
`otpverify:*`가 실제로 걸리는지 알 방법이 현재 없다. §2.2의 IP 상한 튜닝 판단이
막혀 있는 이유가 이것이다.

**권장 조치**: `rate_limit_hit` 실패 시 `alertAdmins`, 버킷 발동 시 최소한
구조화된 `console.warn` + 주기 집계.

## 3.6 Edge Function TypeScript 검사가 CI에 없다 [낮음]

`db-tests.yml`은 pgTAP과 리플레이만 본다. 함수 쪽은 `deploy-functions.yml`이 CLI로
배포할 때 번들링만 하고 **타입 검사를 하지 않는다.**

실제로 이 저장소는 `deno check`를 돌린 적이 없다. 감사 중 확인한 결과:

- `_shared/auth.ts:58` — `Uint8Array` / `BufferSource` 불일치
- `_shared/edge_alert.ts:24,55` — `rpc()` / `insert()` 인자 타입
- `enroll-pet-identity` 단독 **14건**, 그중 `alertAdmins` 인자 타입 오류 4건

전부 **런타임에는 무해**하다(Deno가 타입을 지우고 실행). 하지만 진짜 타입 오류가
섞여 들어와도 구분할 방법이 없다는 게 문제다. #151도 같은 상태에서 머지된다 —
새로 생긴 오류 **클래스**가 없음을 기존 파일과 대조해 확인하는 수동 절차로 대신했다.

**권장 조치**: `db-tests.yml`에 `deno check supabase/functions/*/index.ts` 잡을
추가하되, 커버리지 래칫과 같은 방식으로 **현재 오류 수를 상한으로 고정**하고 줄여 간다
(한 번에 다 고칠 수 없으므로).

## 3.7 `clientIp()`가 DB 경로와 달리 XFF 폴백을 남겨 뒀다 [낮음] — ✅ #151

### 먼저 — IP는 이 배포에서 신뢰할 수 있다

2026-08-01 ADR-0011 증상 4가 운영에 직접 쏴서 측정한 결과:

| 헤더 | 위조 시도 결과 |
| --- | --- |
| `x-forwarded-for` | **통과 — 맨 왼쪽이 공격자 값** |
| `x-real-ip` · `sb-forwarded-for` | 무시됨(엣지가 걷어내고 실제 IP 유지) |
| `cf-connecting-ip` | **엣지가 요청 자체를 거부**(Cloudflare 1000) |

2026-08-03 Edge Function 경로도 같은지 별도 확인했다 — 정상 요청은 함수에 도달(400),
`CF-Connecting-IP` 위조는 엣지에서 거부(1000), `cf-ray` 존재. **경로가 같다.**

따라서 `cf-connecting-ip`를 1순위로 읽는 `otpverify:ip`·`sms:ip`는 **보조선이 아니라
진짜 방어선**이다. 이 문서 초판이 두 버킷을 "스푸핑 가능한 보조"로 적었던 건 오류다
(§2.1 정정 참고).

### 그런데 두 경로의 폴백 정책이 갈려 있다

DB 경로는 XFF 폴백을 **의도적으로 제거**했다
(`20260801140000_ratelimit_trusted_client_ip.sql`, `schema.sql:5247`):

```
-- 익명 식별은 cf-connecting-ip 로만 한다. x-forwarded-for 는 맨 왼쪽이 클라이언트
```

ADR-0011의 표현으로는 *"헤더가 없으면 XFF 로 폴백하지 않는다 — **폴백이 곧 우회로가
되기 때문**이다."*

**그런데 `_shared/auth.ts`의 `clientIp()`는 그 폴백을 그대로 갖고 있다:**

```ts
const trusted = req.headers.get("cf-connecting-ip") ?? req.headers.get("x-real-ip");
if (trusted) return trusted.trim();
const xff = req.headers.get("x-forwarded-for");
return xff ? (xff.split(",")[0].trim() || null) : null;   // ← 위조 가능한 값
```

**지금은 무해하다.** `cf-connecting-ip`가 항상 붙으므로 이 줄에 도달하지 않는다.
Edge Function의 모든 IP 버킷(`login:ip` · `refresh:ip` · `lite:ip` · `sms:ip` ·
`otpverify:ip`)이 실질적으로 위조 불가한 값을 쓴다.

문제는 **도달 불가에 의존하고 있다는 것**이고, 그게 이 문서가 다루는 유형이다.
Cloudflare 앞단이 빠지거나, 다른 오리진으로 직결되거나, 헤더 처리가 바뀌는 순간
**모든 IP 버킷이 조용히 우회 가능해진다.** 아무 오류도 나지 않고 방어선만 사라진다.
DB 쪽이 이미 같은 판단을 내려 폴백을 지운 마당에 엣지 쪽만 남아 있는 것도 일관성
문제다.

부수적으로 `x-real-ip`를 2순위 "신뢰" 헤더로 두는 것도 근거가 약하다. 위 측정은
그 헤더가 **PostgREST 경로에서** 걷어내진다는 것을 보였을 뿐, Edge Function
게이트웨이도 같은지는 재지 않았다. 역시 1순위가 항상 있어 도달하지 않으므로
현재는 무해하다.

### 조치 — #151에 포함 (2026-08-03)

`clientIp()`를 DB 경로와 동일하게 맞췄다. `cf-connecting-ip` **하나만** 쓰고, 없으면
`null`이다:

```ts
export function clientIp(req: Request): string | null {
  const ip = req.headers.get("cf-connecting-ip");
  return ip ? (ip.trim() || null) : null;
}
```

`x-real-ip`도 함께 뺐다. 위 측정은 그 헤더가 **PostgREST 경로에서** 걷어내진다는
것을 보였을 뿐 Edge Function 게이트웨이도 같은지는 재지 않았다 — **재지 않은 것을
신뢰 목록에 두지 않는다**(이 문서가 §7.3에서 반성한 것과 같은 원칙).

**안전한지 호출부를 전수 확인했다.** 헤더가 없으면 `null`이고 호출부는 IP 버킷을
건너뛴다(기존 계약 그대로). IP 버킷을 쓰는 모든 함수에 스푸핑 불가한 1차 버킷이
함께 있어, 건너뛰어도 무방비가 되는 곳이 없다:

| 함수 | IP 버킷 | 함께 걸리는 1차 버킷(스푸핑 불가) |
| --- | --- | --- |
| `login` | `login:ip` | `login:user:<username>` |
| `refresh` | `refresh:ip` | `refresh:tok:<hash>` |
| `signup-lite` | `lite:ip` | `lite:phone:<phone>` |
| `verify-phone-code` | `otpverify:ip` | `otpverify:phone:<phone>:<purpose>` |
| `send-phone-code` | `sms:ip` | phone 60초 조회 + `sms:global` |
| `apply-business` · `check-business-no` · `invite-guardian` | 없음 | `<uid>` · `<phone>` 버킷 |

함수 주석도 실측 결과와 근거(ADR-0011 · 마이그레이션 파일명)로 교체했다.
**이 문서 초판이 그 주석을 1차 근거로 믿고 틀린 서술을 한 것이, 그 문장이 남아
있으면 안 되는 이유였다** — 주석 하나가 감사 문서까지 오염시켰다(§7.3).

---

# 4. 남은 것 — GitHub 저장소 설정

## 4.1 Dependabot 알림 비활성 [중간] — 양쪽 저장소

```
gh api repos/seizeh/pmdart/dependabot/alerts
→ 403 "Dependabot alerts are disabled for this repository."
```

**공개 저장소인데 의존성 취약점 통보가 꺼져 있다.** pmdart는 현재 57개 패키지가
제약 밖 신버전이다. 업데이트를 미루는 건 선택이지만, **취약점이 생겨도 모른다**는 건
다른 문제다. PR 자동생성 없이 알림만 켜도 된다.

## 4.2 Secret scanning 경보 3건 방치 [중간] — pmdart

전부 `state: open`, `publicly_leaked: true`.

| # | 위치 | 키 |
| --- | --- | --- |
| 3 | `lib/firebase_options.dart:44` | `AIzaSyBIuKt-…` |
| 2 | `macos/Runner/GoogleService-Info.plist:6` | `AIzaSyBsa-…` |
| 1 | `android/app/google-services.json:18` | `AIzaSyC6cjm…` |

Firebase 클라이언트 키는 공개 전제가 맞다 — **단, GCP 콘솔에서 애플리케이션 제한
(번들 ID / 패키지+SHA-1)과 API 제한이 걸려 있을 때만.** 제한이 없으면 남이 이 키로
같은 프로젝트의 다른 Google API를 호출해 우리 청구서에 태울 수 있다.

**두 가지를 해야 한다**:

1. GCP 콘솔에서 세 키의 제한 상태 확인(코드로는 판정 불가)
2. 확인 후 경보를 "used in tests / false positive"로 **닫기**

2번이 1번만큼 중요하다. **열린 채로 두면 다음에 진짜 시크릿이 새도 그 알림이 이 셋에
묻힌다.** 알림 채널의 신호대잡음비가 그 자체로 방어선이다.

## 4.3 Actions SHA 미고정 [낮음] — 양쪽

```
allowed_actions: all
sha_pinning_required: false
```

`subosito/flutter-action@v2` · `cloudflare/wrangler-action@v3` · `supabase/setup-cli@v1`
을 **이동 가능한 태그**로 참조한다. 기본 workflow 권한이 `read`라 폭발 반경은 작지만,
`deploy-web`은 `CLOUDFLARE_API_TOKEN`을, `deploy-functions`는 `SUPABASE_ACCESS_TOKEN`을
서드파티 액션에 넘긴다. 태그가 옮겨지면 그 시점부터 토큰이 남의 코드를 지난다.

---

# 5. 남은 것 — 앱(pmdart)

> 이 절은 pmdb 저장소의 관할이 아니다. 감사의 전체 그림을 한 곳에 두기 위해 기록만
> 하고, 실제 후속은 pmdart 이슈로 가야 한다.

## 5.1 realtime 구독 실패가 영구화된다 [중간]

`lib/services/realtime_service.dart:29-53`

```dart
_c.realtime.setAuth(token);
if (_notif != null) return;            // 이미 구독 중
_notif = _c.channel(...).subscribe();  // ← 상태 콜백이 없다
```

`.subscribe()`에 콜백이 없어 `CHANNEL_ERROR` / `TIMED_OUT`을 모른다. 첫 구독이
실패해도 `_notif`는 채널 객체라 non-null이 되고, **이후 모든 `start()`가 조기 리턴하는
no-op**이 된다. 앱을 껐다 켜기 전까지 벨 배지·채팅 목록 실시간 갱신이 죽는다.

FCM 푸시는 별개 경로라 계속 오므로 사용자는 *"가끔 배지가 안 맞네"* 정도로만 느끼고
신고하지 않는다.

**조치**: `subscribe((status, err) {...})`로 실패 시 `_notif = null` + 백오프 재구독 +
`ErrorReporter.report`.

## 5.2 목록 쿼리가 1000행에서 소리 없이 잘린다 [중간]

`lib/services/` 기준 `.select()` **67곳**, `.limit()/.range()` **7곳**.

| 파일 | select | limit |
| --- | --- | --- |
| `pet_repository.dart` | 13 | **0** |
| `profile_repository.dart` | 12 | **0** |
| `activity_repository.dart` | 10 | **0** |
| `community_repository.dart` | 11 | 1 |

`fetchComments(postId)` · `fetchUserPosts` · `fetchHeartedPosts` ·
`fetchMyApplications` · `fetchApplicants` · `fetchBlockedUsers` 전부 전량 조회다.

### 서버 상한은 있다 — 실측 결과 `db-max-rows = 1000`

2026-08-03 운영 REST 로 직접 확인했다(`facilities`, 전체 24,552행):

| 요청 | 반환 행 |
| --- | --- |
| `?select=id` (상한 미지정) | **1000** |
| `?select=id&limit=5000` | **1000** |
| `Range: 0-4999` | **1000** |
| `Range: 1000-1999` | 1000 (`content-range: 1000-1999/*`) |

즉 클라이언트가 무엇을 요구하든 한 응답은 1000행에서 끊긴다. offset 페이지네이션은
정상 동작한다.

### 그래서 성격이 바뀐다 — 자원 문제가 아니라 정합성 문제다

처음에 이 항목을 "상한 없음"으로 잡았던 건 **틀렸다.** 5,000건을 한 번에 끌어와
메모리·대역폭이 터지는 시나리오는 서버가 이미 막고 있다.

**대신 더 나쁜 게 남는다 — 조용한 절단이다.**

`fetchComments`가 댓글 1,200건인 글에서 1,000건만 받아 온다. 오류도, 경고도, 아무
신호도 없다. **1001번째 댓글부터는 "실패"가 아니라 "존재하지 않음"으로 보인다.**
앱은 목록이 불완전하다는 사실 자체를 모르고, 사용자는 자기 댓글이 사라졌다고 느낀다.

이 프로젝트는 같은 실패 모드를 이미 한 번 겪고 이름을 붙였다 — pmdart#234,
*"조용히 폴백하면 하이퍼로컬 전제가 깨진 채 멀쩡해 보이는 전국 피드가 나간다.
오류가 데이터처럼 보이는 최악의 실패 모드"*. 그때는 오류를 데이터로 착각하는
문제였고, 이번엔 **잘린 데이터를 전부라고 착각하는** 문제다. 구조가 같다.

증상이 "없는 것처럼 보임"이라 **§0의 사각지대 정의에 오히려 더 잘 맞는다.**
"상한 없음"이었다면 언젠가 느려지거나 죽어서 드러나기라도 하는데, 이건 영원히
정상으로 보인다.

### 언제 터지나

1000은 지금 데이터에서는 멀어 보이지만 **행 단위가 아니라 관계 단위로 세야 한다**:

- 인기 글 하나의 댓글 — 가장 먼저 닿는다
- 오래 쓴 계정의 `fetchUserPosts` · `fetchHeartedPosts`
- 시설 하나의 후기 누적(매장 QR이 돌기 시작하면)

### 별개로 `fetchFeed`

`.limit(100)`이라 서버 상한과 무관하게 **101번째 글부터 영영 안 보인다.** 페이지네이션
자체가 없다. pmdart#30("동네 게시글 시트: 게시글 폭주 대비 '더보기 → 커뮤니티' 흐름")이
같은 압력의 다른 면을 다루고 있다 — 그쪽은 시트의 표시 전략이고, 이건 피드 조회 자체의
상한이다. 둘 다 "글이 많아지면"이 조건이라 같은 시점에 터진다.

### 권장 조치

행 수를 늘리는 게 아니라 **절단을 감지 가능하게** 만드는 쪽이다.

1. 목록 조회에 명시적 `.range(from, to)` + `Prefer: count=exact`로 전체 건수를 받아
   "더 있음"을 앱이 **알 수 있게** 한다
2. 최소한 반환 행이 요청 상한과 같으면(=잘렸을 가능성) 더보기 UI를 띄우거나
   `ErrorReporter`로 남긴다
3. 1000에 실제로 닿는 경로(댓글이 유력)부터 페이지네이션

## 5.3 검색이 인덱스를 못 쓰는 선행 와일드카드 [중간]

`community_repository.dart:119`

```dart
q.or('title.ilike.%$term%,content.ilike.%$term%')
```

`%`로 시작해 B-tree를 못 쓴다 → `v_post_feed` 전량 스캔. 게시글 수천 건에서 검색만
수 초가 된다. **pmdb 쪽 조치가 필요한 항목**이다 — `pg_trgm` GIN 인덱스 또는 tsvector.

(주입 쪽은 `[,()%*]`를 걷어내 조건 추가가 막혀 있다. 문제는 성능뿐이다.)

## 5.4 접근성 전무 [중간]

- 51,195줄에 `Semantics` / `semanticLabel` **0곳**. 아이콘 전용 버튼(하트·더보기·
  닫기·뒤로)이 TalkBack·VoiceOver에서 아무것도 읽히지 않는다.
- `fontSize:` 하드코딩 **577곳**, textScaler 클램프 없음. OS 글자 크기를 크게 쓰는
  사용자에게 레이아웃이 넘친다.

개발자는 기본 배율·비보조기술로만 보므로 **구조적으로 안 보인다.** 국내 모바일앱
접근성 지침과 스토어 심사에서도 지적될 수 있다.

## 5.5 Android auto backup 미차단 [중간]

`AndroidManifest.xml`에 `android:allowBackup="false"`도 `android:dataExtractionRules`도
없다 → 기본값 `true`.

`flutter_secure_storage`(EncryptedSharedPreferences)와 SharedPreferences가 Google
드라이브 백업에 포함된다. 새 기기로 복원하면 **Keystore 키는 따라오지 않아** 복호화가
실패한다 — `session.load()`가 깨진 값을 읽고 로그인이 조용히 풀린다. 겸사겸사
access/refresh 토큰이 백업 매체로 나간다.

`session.dart`가 반쪽 세션 정리(pmdart#239)까지 신경 쓴 코드인데, 이 경로는 그 앞단이다.

## 5.6 release 서명이 debug로 조용히 폴백 [중간]

`android/app/build.gradle.kts:59`

```kotlin
signingConfig = if (keystoreProperties.isNotEmpty())
    signingConfigs.getByName("release")
else
    signingConfigs.getByName("debug")   // ← 경고 한 줄 없음
```

`key.properties`가 없는 머신에서 `flutter build appbundle --release`가 **성공하고**
debug 키로 서명된 산출물이 나온다. Play 업로드에서는 거절되지만, 직접 배포용 APK라면
설치는 되고 나중에 업그레이드가 막힌다.

## 5.7 iOS `aps-environment: development` [중간]

`ios/Runner/Runner.entitlements`가 development로 고정. TestFlight/App Store 빌드에서
프로비저닝이 production으로 치환해 주지 못하면, FCM은 production APNs로 보내는데
토큰은 sandbox라 **푸시가 전부 조용히 유실**된다.

개발 기기에서는 완벽하게 동작하므로 전형적인 사각지대다. **TestFlight 빌드 실기기로
푸시 1회 수신을 직접 확인해야 한다.**

## 5.8 웹 푸시 SW의 Firebase SDK 버전 수동 고정 [낮음]

`web/firebase-messaging-sw.js`가 `11.9.1`을 하드코딩한다. `firebase_core_web`의
`supportedFirebaseJsSdkVersion`과 어긋나면 웹 푸시 토큰 발급이 조용히 실패한다.

파일 주석이 그 위험을 정확히 알고 있는데 **검증은 사람 몫으로 남아 있다** —
`pubspec` 업그레이드 시 이 파일을 같이 고치게 강제하는 CI 체크가 없다.

## 5.9 웹 응답에 CSP 외 보안 헤더 없음 [낮음]

`web/_headers`에 CSP는 정성껏 들어가 있지만 `X-Content-Type-Options` ·
`Referrer-Policy` · `Strict-Transport-Security` · `Permissions-Policy`가 없다.

특히 Referrer-Policy 부재로 `/p/<postId>` 공유 링크가 `fonts.gstatic.com` 요청의
`Referer`에 그대로 실려 나간다.

---

# 6. 남은 것 — 관측

## 6.1 오류를 모으는데 알려 주는 장치가 없다 [중간]

`app.client_errors` + `admin_client_error_summary`(0011 ADR)까지 만들어 놨는데,
**누군가 관리자 화면을 열어야만** 안다. "새 배포 직후 특정 `where`가 급증" 같은 걸
잡아 줄 임계 알람이 없다. `Env.appRelease`를 넣어 둔 이유가 딱 그건데 소비하는 쪽이
없다.

같은 축의 미관측 신호들: 로그인 실패 빈도, 레이트리밋 발동(§3.5), refresh 거절 빈도,
`rate_limit_hit` 자체의 실패.

**재료는 이미 있다** — `_shared/edge_alert.ts`의 `alertAdmins()`. pg_cron 스윕 하나로
"최근 1시간 특정 where 급증 → 관리자 알림"을 붙일 수 있다. #151이 전역 SMS 상한에
같은 패턴을 처음 적용했다.

## 6.2 커버리지 19.1%, 41개 파일이 0% [낮음]

0%인 곳이 하필 실패 비용이 큰 쪽이다:

```
315줄  lib/services/admin_repository.dart
115줄  lib/services/activity_repository.dart
112줄  lib/services/care_report_repository.dart
 74줄  lib/services/push_service.dart
 39줄  lib/services/vaccination_repository.dart
```

전체 비율 래칫(≥18%, ADR 0007)은 좋은 장치인데 **새로 추가된 파일이 0%여도 통과한다.**
"신규 파일은 0% 금지" 축이 하나 더 있으면 이 목록이 안 늘어난다.

---

# 7. 후속 작업

## 7.1 우선순위

| 순위 | 항목 | 근거 |
| --- | --- | --- |
| 1 | §3.1 Edge Function status/tv 게이트 | 정지·회수가 실제로 안 먹는다. `verifyAccess` 한 곳 + 사본 6개 통일 |
| 2 | §4.1 Dependabot · §4.2 secret 경보 정리 | 클릭 몇 번, 알림 신뢰도 회복 |
| 3 | §3.2 유료 API 레이트리밋 | §3.1과 함께 고치면 한 번에 |
| 4 | §3.5 레이트리밋 관측 | §2.2 IP 상한 튜닝의 선행 조건 |
| 5 | §5.1 realtime 재구독 | 조용한 기능 사망 |
| 6 | §3.3 DEMO 백도어 해제 확인 | 심사 통과 여부에 달림 |
| 7 | §5.5 §5.6 §5.7 | 출시 직전 확인 묶음 |

## 7.2 코드로 판정 불가 — 사람이 확인해야 하는 것

1. **GCP 콘솔** — Firebase API 키 3개에 애플리케이션/API 제한이 걸려 있는지(§4.2)
2. **Supabase 시크릿** — `DEMO_PHONES` / `DEMO_OTP`가 아직 설정돼 있는지(§3.3)
3. **TestFlight 실기기** — 푸시가 실제로 도착하는지(§5.7)

### 해소된 것

4. ~~**PostgREST 설정** — `db-max-rows`가 걸려 있는지(§5.2)~~
   → **2026-08-03 실측 완료: `db-max-rows = 1000`.** 운영 REST 에 직접 쏴서 확인했다
   (`facilities` 24,552행 → 무제한 요청·`limit=5000`·`Range: 0-4999` 모두 1000행 반환).
   결론이 뒤집혔다 — 자원 고갈은 막혀 있고, **조용한 절단**이 남았다. §5.2 참고.

5. ~~**IP 버킷이 스푸핑 가능한 보조선인지**~~
   → **2026-08-03 확인 완료: 아니다.** `cf-connecting-ip` 위조는 Cloudflare 엣지가
   요청 자체를 거부하고(에러 1000), Edge Function 경로도 동일하다(`cf-ray` 존재).
   ADR-0011 증상 4의 측정과 일치한다. §3.7 참고.

## 7.3 이 문서가 만든 부채

- `verify-phone-code`의 헤더 주석이 아직 `verify_jwt=false`라고 적고 있다.
  `config.toml`은 `true`다. #151이 `send-phone-code` 쪽만 고쳤다.
  (`docs/supabase-api.md` §6(412행)이 세 함수 모두를 지목해 둔 항목 중 나머지 하나.)
- `docs/supabase-api.md`가 #151의 새 레이트리밋을 아직 반영하지 않았다.
  **머지 후 갱신할 곳**(현재 줄 번호):
  - §4 `### send-phone-code`(136행) — "인증: 없음(로그인 전 단계). 남용은 자체
    레이트리밋으로 방어"(139행)를 세 층 표로, §1 개요표(17행)의 "전화번호 자체
    레이트리밋"도 함께
  - §4 `### verify-phone-code`(153행) — 내부 로직에 대입 제한 단계 추가, 응답에
    `429 rate_limited` 추가
  - §2.1 `_shared/auth.ts`(50행) — `rateLimited` 사용처 목록에 두 함수 추가.
    **같은 절 `clientIp(req)` 설명(58행)도 함께 고쳐야 한다** — "`cf-connecting-ip`
    → `x-real-ip` 우선, `x-forwarded-for` 최후 폴백 / IP 제한은 보조 방어선일 뿐"이
    #151 이후 전부 사실과 다르다(§3.7)
  - §2.4 `_shared/edge_alert.ts`(79행) — `alertAdmins` 사용처에 `sms-global-cap` 추가
- §2.2의 `IP_MAX` 값은 **미결**이다. §3.5가 선행되지 않으면 근거를 갖고 조정할 수 없다.
  (IP 신뢰도가 확인되면서 완화 쪽으로 기울었지만, CGNAT 실측 없이 숫자를 옮기는 건
  근거 없는 이동이라 그대로 둔다.)
- ~~`_shared/auth.ts`의 `clientIp()` 주석("IP 제한은 보조 방어선일 뿐 — 스푸핑 가능")이
  실측과 어긋난다.~~ → **#151에서 XFF 폴백 제거와 함께 주석도 교체(§3.7).**
  남겨 둘 기록: **이 문서 초판이 그 주석을 1차 근거로 믿고 §2.1·§2.2에 틀린 서술을
  넣었다.** 주석 하나가 감사 문서까지 오염시킨 사례다.

### 이 문서가 스스로 밟은 함정

ADR-0011 증상 4의 교훈은 *"'못 속인다'는 문장을 쓸 때는 속여 보고 나서 쓴다"* 였다.
초판은 그 반대를 했다 — **"속일 수 있다"를 재 보지 않고 썼다.** 방향만 반대일 뿐
같은 오류다. 코드 주석을 1차 근거로 취급한 게 원인이고, 사용자가 운영에 직접 쏴서
잡았다.

`db-max-rows`도 같은 구조였다. "확인이 필요하다"로 미뤄 뒀는데 **REST 요청 두 번이면
끝나는 것**이었고, 재 보니 결론이 뒤집혔다(상한 없음 → 1000행 조용한 절단). 감사
문서에서 "확인 필요"는 재 볼 수단이 없을 때만 쓸 말이다.

## 7.4 관련 PR

| PR | 내용 |
| --- | --- |
| pmdb #151 | 전화 OTP 대입 제한 + 문자 발송 총량 상한 (§2.1 §2.2) |
| pmdb #152 | 함수 배포를 DB Tests 통과 뒤로 (§2.3) |
| pmdart #254 | 웹 배포를 CI 통과 뒤로 (§2.3) |
| pmdart #255 | 대입 제한 429의 사용자 문구 (§2.4) |
