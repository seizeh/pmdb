# 0032 — 0031 조치 기록: 있는데 안 막던 것들

> 상태: **조치 완료·전량 배포**. 작성 2026-08-04.
> 대상: pmdb(DB·Edge Functions·CI) + pmdart(앱·빌드) + pmlegal(법무 정본).
> 관련 PR: pmdb #151~#162 · pmdart #253~#266 · pmlegal #14 #18 #19 (전부 머지).
>
> 0031 은 **"무엇이 이미 잘못돼 있는데 아무도 모르고 있었나"** 의 목록이었다.
> 이 문서는 그 후속인데, 성격이 하나 더 붙었다 — 0031 이 세운 목록을 처리하는
> 과정에서 **0031 의 분류 자체가 놓친 축**이 드러났고, 그쪽이 더 위험했다.

## 0. 이 조치가 드러낸 것 — 0031 에 없던 다섯 번째 유형

0031 은 사각지대를 네다섯 유형으로 나눴다: 공격자만 밟는 경로 · 비용으로만 드러남 ·
조용히 죽는 경로 · 규모가 오기 전엔 잠복 · 도달 불가에 의존.

**전부 "방어가 없는 곳"의 분류다.** 그런데 이번에 나온 상위 항목은 전부 다른 축이었다.

> **부분 적용** — 같은 불변식을 지켜야 할 경로가 N개인데 그중 일부만 구현됐다.
> 구현된 경로가 정상 사용 경로라서 테스트도 실사용도 늘 그쪽만 밟는다.

| 사례 | 있던 방어 | 안 막던 경로 |
|---|---|---|
| §1.1 간이 토큰 | "계정이 `status='lite'` 라 후기밖에 못 쓴다" | 기존 **정식 회원**은 `active` 라 전부 통과 |
| §1.2 재설정 인증 | "30분 내 인증된 번호만" | 인증을 **소진하지 않아** 30분간 재사용 |
| §1.3 사진 게이트 | 트리거가 카운터를 올린다 | 카운터를 **RPC 밖에서** 올릴 수 있었다 |
| §1.4 푸시 토큰 해제 | "서버 먼저, 기기 나중"(#237) | 강제 로그아웃 경로에서만 **순서가 역전** |
| §2 차단 | 표시 뷰 3개가 상대를 가린다 | 알림·팔로우·상호작용은 **그대로** |
| §4.1 정지·회수 게이트 | `app.uid()` 가 매 요청 검사 | service_role 함수는 **RLS 를 우회** |

0031 도 §3.1(자체 JWT 검증 사본 6개)에서 같은 패턴을 잡았지만 **유형으로 승격하지
않았다.** 그래서 같은 모양의 다른 사례들이 목록에 안 들어왔다.

이 렌즈로 보면 다음 후보가 바로 보인다 — `user_blocks` 를 참조하는 곳 vs 참조해야
할 곳, 보상 로직(`discardByUrl`)이 있는 화면 vs 없는 화면, 요청 순서 가드가 있는
화면 vs 없는 화면. 실제로 그 셋 다 이번에 결함으로 확인됐다.

---

# 1. 실제 취약점 (전부 운영 DB·배포본으로 직접 검증)

## 1.1 간이 후기 토큰이 정식 회원 계정에 완전 세션을 발급했다 — pmdb #154

**대상**: `signup-lite/index.ts` · `public.signup_lite_user` · `app.uid()`

### 무엇이 문제였나

`signup_lite_user` 는 같은 번호의 **기존 계정을 그대로 돌려준다**(0029 의 의도 —
방문 회차 `visit_no` 가 끊기지 않게). 정지·탈퇴만 거부하므로 `status='active'`
정식 회원도 반환된다. 그리고 엣지가 그 계정의 **실제 `token_version` 을 정확히
stamp** 해 서명한다 — 안 하면 tv 가 오른 계정이 즉시 잠기므로 이 처리 자체는 필요하다.

그 결과 발급되는 15분 토큰이 `app.uid()` 의 조건(`status='active'` + tv 일치)을
**완전히 충족했다.** 후기가 아니라 채팅·게시글·펫·프로필 전부에 통했다.

함수 주석과 `docs/supabase-api.md:203` 은 정반대를 적고 있었다 —
*"이 토큰으로는 후기 작성 외에 아무것도 못 한다 — 계정이 `status='lite'` 라"*.
`status='lite'` 인 **신규** 계정에만 참이고 기존 정식 회원에는 거짓이다.

### 왜 심각한가 — 문턱이 아니라 소란스러움의 차이

전제는 그 번호로 오는 SMS 를 볼 수 있어야 한다는 것이고(`send-phone-code` 는
`purpose='review'` 에 계정 존재를 검사하지 않는다 — 0029 의 의도된 설계), 그 문턱은
비밀번호 재설정과 같다. 다른 건 **흔적**이다.

| | 재설정 | 이 경로 |
|---|---|---|
| 비밀번호 | 바뀐다 | 그대로 |
| `token_version` | 오른다 | 그대로 |
| refresh | 전량 회수 | 유지 |
| `auth_logs` | — | 미기록 |
| 새 기기 로그인 알림 | 발송 | 없음 |
| 피해자 인지 | **즉시 잠겨 알아챈다** | **아무 흔적 없음** |

통신사 번호 재배정처럼 "남의 번호를 합법적으로 갖게 되는" 경우가 현실에 있으므로
조용한 쪽을 열어 두면 안 된다.

### 조치 — 권한 판정을 계정 상태가 아니라 토큰에 싣는다

`signup-lite` 가 `lite: true` 클레임을 박고 `app.uid()` 가 그것을 거부한다.
`app.uid_lite()` 는 그대로 받으므로 후기 3종(`add_facility_review` ·
`facility_review_by_id` · `facility_reviews_of`)은 계속 동작한다.

**같은 계정이 경로에 따라 다른 권한을 가져야 하는데 계정 행은 경로를 모르고 토큰은
안다.** 그게 판정 근거를 옮긴 이유다.

클레임은 텍스트로 비교한다(`<> 'true'`). `::boolean` 캐스팅이면 예상 밖 값에서
**캐스트 오류**가 나고, 그 오류는 RLS 정책 안에서 터지므로 "거부" 가 아니라
"쿼리 실패" 가 된다.

### 곁들여 — 간이 후기 사진 업로드는 원래 깨져 있었다

`media` 버킷 쓰기 정책이 `app.uid()` 기준이라 **`status='lite'` 인 진짜 간이 계정은
처음부터 사진을 못 올렸다**(`app.uid()` 가 NULL → 폴더명 비교가 NULL → 거부).
앱은 올릴 수 있다고 가정했다 — `facility_review_screen` 의
*"인증을 마쳤으니 이제 Storage 에 쓸 수 있다"* 주석이 그 기대다.

즉 그 기능은 **정식 회원이 간이 경로로 들어온 경우에만**, 그것도 위 결함 덕에
우연히 동작하고 있었다. 위 조치로 그 우연이 사라지므로 같은 마이그레이션에서
의도된 동작을 만들었다 — 자기 폴더 아래 `facility_review/` 한 칸만 `uid_lite` 로 허용.

## 1.2 비밀번호 재설정 인증이 소진되지 않았다 — pmdb #154

**대상**: `public.reset_password_user` · `reset-password/index.ts`

`reset_password_user` 는 *"phone + `purpose='password_reset'` + `is_used=true` +
30분 이내 행이 **존재하는가**"* 만 봤다. 코드값을 다시 받지도(엣지가 `code` 를 인자로
받지 않는다) 그 행을 소진하지도 않았다. **인증 1회가 30분짜리 열쇠**였다.

피해자가 정상 재설정을 마치면 그 후 30분 동안 번호만 아는 제3자가
`{phone, new_password}` 만 보내 비밀번호를 덮어쓸 수 있었다.

### 더 나쁜 것 — 무료 오라클

이 엔드포인트에는 레이트리밋이 없었다(#151 도 이 함수는 건드리지 않는다). 그래서
`403 phone_not_verified` 와 `200 ok` 의 차이가 **"이 번호가 최근 30분 안에 재설정을
했는가"** 를 공짜로 알려줬다. 번호 목록을 계속 찔러 보며 창이 열리기를 기다릴 수 있었다.

### 경로 간 비대칭이 근거였다

같은 비소진 RPC 를 쓰는 다른 둘은 각자 창을 닫아 두었다:

- `signup-lite` — 엣지가 코드 일치를 직접 확인하고 `is_used` 를 찍는다(창 없음)
- `signup` — `users_phone_uq` 가 2차 방어(같은 번호로 두 번 못 만든다)

**`reset-password` 만 어느 쪽도 없었다.**

### 조치

비번을 바꾼 뒤 같은 트랜잭션에서 그 번호의 `password_reset` 인증을 지운다. 남는 창은
"인증 완료 ~ 정상 사용자가 새 비번 제출" 사이 수 초이고, 그 수 초를 노린 폴링에
값을 매기려 엣지에 레이트리밋을 넣었다(`pwreset:phone` 10/10분 + `pwreset:ip` 30/10분).

앱 문구도 함께(#257) — 없으면 기본값 *"비밀번호 재설정에 실패했어요"* 로 떨어지는데,
그건 *틀렸다*는 뜻으로 읽혀 사용자를 같은 자리에서 계속 재시도하게 만든다.

## 1.3 post_pets 직접 조작으로 사진 인증 게이트를 넘을 수 있었다 — pmdb #154

게이트 판정은 값이 **정확히** 그 셋일 때만 참이다:

```sql
app.needs_photo_gate(n) := n in (0, 3, 9)   -- 펫별 1·4·10번째 글
```

`pets.verify_post_count` 는 `app.tg_post_pets_bump_verify_count()` 가 post_pets
INSERT 마다 +1 한다. DELETE 분기가 없는 건 **의도된 것**이다(20260728103000 —
*"지웠다 다시 쓰는 방식으로 게이트를 피하지 못하게"*).

그런데 과대계상 방향이 고려되지 않았다:

- `authenticated` 에 post_pets `INSERT`/`DELETE` GRANT 존재
- 정책은 둘 다 조건이 *"내가 쓴 글이면 OK"* 뿐
- `post_pets_uq` 는 DELETE 하면 풀려 재삽입 가능

```
자기 글에 (post_id, pet_id) 삽입 → 삭제 1회  ⇒  verify_post_count 3 → 4
→ create_post_verified 의 v_exempt 가 참 → app.photo_trusted='true'
→ tg_posts_check_write 가 사진 토큰 검사를 통째로 스킵
```

REST 두 번이면 끝난다. 9(10번째)도 같다.

### 왜 DELETE 에서 감소시키지 않았나

그건 원래 막으려던 우회를 되살린다. 실제 결함은 **카운터를 RPC 밖에서 움직일 수
있다는 것**이다. 20260728103000 의 주석은 *"RPC 밖(직접 INSERT) 경로도 새지 않도록
트리거로"* 라고 적었는데, 그 직접 경로가 카운터를 **올리는** 데도 쓰인다는 걸 놓쳤다.
**방어를 트리거로 옮기면서 정작 트리거를 발화시키는 문을 잠그지 않았다.**

앱도 post_pets 는 조회만 하고 쓰기는 `create_post_verified`(definer)가 한다.
쓰기 권한을 회수했다. RLS 정책은 남겨 뒀다 — 권한이 되돌려져도 남의 글은 못 건드리게.

## 1.4 강제 로그아웃 시 서버 푸시 토큰 해제가 실행되지 않았다 — pmdart #257

`SessionManager._invalidate()` 가 `await clear()` **다음에** `onInvalidated` 를 부르는데,
그 핸들러가 `PushService.clearToken()` 을 호출하고 그 안의 서버 해제는
`isLoggedIn` 가드 안에 있었다. `clear()` 가 이미 끝났으므로 **`release_device_token`
RPC 가 통째로 스킵**되고 기기측 `deleteToken()` 만 돌았다.

`push_service.dart:153-163` 이 #237 에서 명문화한 **"서버 먼저, 기기 나중" 의 정확한
역전**이다. 적용 대상은 정상 로그아웃을 뺀 **모든** 세션 종료 — 타 기기 비번 변경,
계정 정지, refresh 회수, 8시간 만료.

`AuthService.logout()` 은 clear 전에 부르므로 **정상 경로만 올바랐고, 그래서
눈에 띄지 않았다.**

조치: `onBeforeInvalidate` 훅을 `clear()` **전에** 부른다. 뒷정리가 실패해도 세션
정리는 진행돼야 하므로 예외는 삼키되 `ErrorReporter` 로 남긴다.

## 1.5 비밀번호 변경이 복잡도 규칙을 우회했다 — pmdb #158 · pmdart #263

세 경로의 규칙이 갈려 있었다.

| 경로 | 검사 | 문제 |
|---|---|---|
| 가입(앱) | 길이 8자만 | 화면 힌트는 "영문 + 숫자 포함" 인데 **검사는 길이만** |
| 재설정 | 8자 + 영문 + 숫자 | 정상 |
| **변경** | **6자만** | **가입에서 막은 비밀번호를 '변경' 으로 우회** |

서버도 `change-password` 만 6자였고, 주석이 스스로 *"구 `app._set_password` 정책 유지"*
라고 밝히고 있었다 — 잔재인 걸 알면서 정책이 갈린 채로 굳었다.

`lib/utils/password_rule.dart` 를 정본으로 두고 세 화면 + `AuthResult` 문구가 같은
상수를 쓰게 했다. 힌트와 에러 문구가 한 곳에서 나오므로 **안내와 검사가 다시 갈릴 수
없다.** 서버 세 함수도 같은 규칙으로 맞췄다.

**이 버그는 테스트에 화석으로 남아 있었다** — 기존 테스트가
`expect(msg('weak_password'), '새 비밀번호는 6자 이상이어야 해요')` 로 옛 문구를
못 박고 있었다. 즉 테스트가 버그를 지키고 있었다.

DB 계층에는 정책이 없음을 확인했다 — `change_password_and_rotate` ·
`reset_password_user` · `update_password_hash` 셋 다 **이미 해시된 값을 받으므로**
길이·복잡도를 검사할 수 없고 하지도 않는다. 규칙이 사는 곳은 엣지 3개 + 앱 사본뿐이다.

> ⚠️ 이 수정은 2026-08-01 에 작업돼 **푸시조차 되지 않은 채 로컬에만** 있었다.
> 그동안 구멍은 열려 있었고 수정본은 한 머신에만 존재했다. 브랜치가 main 대비
> 45커밋 뒤처져 있어 체리픽으로 되살렸다.

---

# 2. 차단 기능 — 표시만 하고 접촉은 끊지 않았다 (pmdb #155 · pmdart #258)

첫 App Store 심사 거절 사유가 1.2(차단 부재)였고 그 대응으로 만든 기능인데,
`user_blocks` 는 스키마 전체에서 **다섯 곳**에만 쓰이고 있었다 — 표시 뷰 3개
(`v_post_feed`·`v_comment_feed`·`v_chat_rooms`) + 채팅 전송 거부 + `block_user` 자신.

| # | 구멍 | 증상 |
|---|---|---|
| 1 | `pawings`(팔로우)가 남는다 | 내가 글을 올릴 때마다 크론이 **차단한 상대에게** 새 글 푸시를 보낸다 |
| 2 | 알림 생산자 12개에 차단 검사 0건 | 차단한 상대의 하트·댓글이 **그 사람 닉네임이 박힌 푸시**로 온다. 댓글 본문은 뷰가 가리므로 *알림만 오고 내용은 없는* 상태 |
| 3 | 가려진 채팅방의 안읽음이 남는다 | 방이 목록에 없어 **열어서 읽을 수도 없고**, `reconcile_unread_counts` 도 가려진 방까지 순회해 같은 값을 다시 계산하므로 **보정으로도 못 지운다** |

`20260801130000` 이 *"게시글 피드만 걸렀다 … 채팅이 전혀 안 막혀 있었다"* 며 범위를
넓힌 이력이 있다. 같은 성격의 미완이 남아 있었다.

**왜 아무도 몰랐나** — 차단은 개발 중에 거의 실행되지 않고, 실행해도 확인하는 건
"목록에서 사라졌나" 뿐이다. 위 셋은 전부 차단한 **뒤 시간이 지나야** 드러난다
(상대가 다음 글을 올릴 때, 다음 하트를 누를 때). 그 시점엔 차단과 연결 짓기 어렵다.

## 조치 — 채팅이 이미 쓰던 구조를 넓혔다

**A. 원천 거부** — 하트·댓글 INSERT 자체를 막는다(`chat_block_blocked_user` 와 같은
모양). 알림만 막으면 하트 수는 올라가고 댓글 행은 저장되는데 아무도 모르는 상태가 된다.

**B. 안전망** — 차단 쌍 사이의 알림 행을 아예 만들지 않는다(`notifications` BEFORE INSERT).

B 가 따로 필요한 이유: A 로는 크론이 만드는 `pawing_new_post` 나 후기·지원·초대처럼
"직접 접촉이 아닌데 알림은 가는" 경로를 못 막는다. **생산자가 12개라 하나씩 고치면
다음에 추가되는 13번째가 또 빠진다** — 행이 만들어지는 길목에 한 번 건다.
`actor_user_id` 가 12개 중 11개에 채워져 있어 그 한 곳으로 커버된다.

B 는 **예외를 던지지 않고 조용히 건너뛴다.** 이 트리거는 남의 트랜잭션 안에서 도는데,
여기서 raise 하면 알림을 만들려던 원래 작업이 통째로 실패한다. 알림은 부수 효과이므로
부수 효과가 본 작업을 죽이면 안 된다.

`application_accepted` 만 `actor_user_id` 를 안 채워 그물을 통과한다 — **의도적으로
둔다.** 사회적 접촉이 아니라 내가 낸 지원의 결과 통보이고, 차단했다고 안 알려주면
사용자가 상태를 알 방법이 없어진다.

## 차단 시점 뒷정리

- **`pawings` 양방향 삭제.** 그물이 푸시는 막지만 관계를 남겨 두면 차단 해제 시 예전
  팔로우가 되살아나 "왜 이 사람이 나를 팔로우하지" 가 된다. 이전 차단분도 소급 정리.
- **가려진 채팅방을 마지막 메시지까지 읽음 처리.** `leave_chat_room` 이 나갈 때 하는
  것과 같은 처리이고, 카운터 감소는 `trg_chat_members_read`(BEFORE UPDATE)가 알아서
  한다. 양쪽 다 처리하는 이유는 뷰가 **양방향**으로 방을 가리기 때문.

공통 술어는 `app.is_blocked_pair(uuid, uuid)` 하나로 두고 사본을 만들지 않았다 —
**차단 필터가 다섯 곳에 흩어져 있던 게 애초에 이 미완의 원인이다.**

## 앱 쪽 — "차단했어요" 직후 그 사람 글이 그대로 보였다

`report_repository.block()` 의 주석은 *"서버 뷰가 즉시 걸러내므로 클라이언트는 목록만
새로 받으면 된다"* 고 정확히 적고 있었는데 **그 "새로 받는" 트리거가 없었다.**
각 화면은 자기 자신만 pop 하고, `community_tab._openPost` 는 상세에서 돌아와도
`_load()` 를 부르지 않는다.

비대칭이 근거였다 — 해제(`unblock`)는 `notifySocial()` 을 쏘고 있었다. 차단만 빠졌다.
그리고 그 해제조차 반쪽이었다(커뮤니티 탭은 `feed` 만 구독).

차단이 동시에 바꾸는 세 곳(`feed`·`chat`·`social`)에 맞춰 셋을 쏘고, 해제도 대칭으로
맞췄다. **상태를 되돌리는 연산이 만드는 연산과 다른 이벤트를 쏘면, 되돌렸는데 화면이
안 따라오는 자리가 반드시 생긴다.**

---

# 3. 0031 이 스스로 만든 부채 — #151 머지 전 정리

0031 §7.3 은 *"주석 하나가 감사 문서까지 오염시켰다"* 고 반성했는데, **같은 실패
모드를 같은 PR 이 새 파일 두 곳에 재생산하고 있었다.**

`send-phone-code:27` 과 `verify-phone-code:20` 이 *"IP 는 스푸핑 가능한 보조선이다
(clientIp 주석 참고)"* 라고 적으면서 **clientIp 주석을 근거로 지목**했는데, 같은 PR 의
두 번째 커밋이 그 주석에서 정확히 반대를 확인했다. 첫 커밋의 문장을 두 번째 커밋이
갱신하지 않은 것이다.

같은 이유로 `docs/supabase-api.md` §2.1 의 `clientIp` 설명도 고쳤다 — 그대로 두면
6개 PR 을 다 머지해도 **문서가 완전히 거짓**인 채로 남는다.

## `retry_after_sec` 이 실제 창의 1/60 이었다

②(`sms:ip`)와 ③(`sms:global`)은 창이 **3600초**인데 응답은 60 을 실었다. 앱은 그
값을 그대로 *"N초 후 재발송 가능"* 으로 띄운다 — 최대 1시간 막힌 사용자에게 60초 뒤
다시 누르라고 안내하고, 눌러도 또 막힌다. **이 PR 이 검증 쪽에서 고치려던 문제와
정확히 같은 종류**다.

앱은 대기 시간의 자릿수에 따라 문장 자체를 바꾸게 했다(#255) — 분 이상이면 "잠시 후"
를 쓰지 않는 게 요점이다. 고정창이라 실제 대기는 더 짧을 수 있어 '최대' 로 적었다.

## 데모 백도어가 절반만 열려 있었다

`send-phone-code` 는 데모 번호를 레이트리밋에서 면제하는데(*"심사 중 재시도 마찰을
없앤다"*) `verify-phone-code` 에는 `isDemo` 분기 자체가 없어 새 제한에 그대로 걸렸다.
**App Review 재제출이 진행 중이고** 심사자는 같은 번호로 인증을 반복한다 —
발급은 되는데 검증만 막히는 상태가 가장 나쁘다(증상이 "인증이 안 된다" 라 앱 결함으로 보인다).

## `verify_jwt` 주석이 config.toml 과 반대

`verify-phone-code:5`·`signup:8` 이 `verify_jwt=false` 라고 적는데 `config.toml` 은
둘 다 `true` 였다. 고치면서 *"그래서 verify_jwt 가 방어냐"* 도 적었다 — 아니다.
publishable 키는 앱 번들에 실려 있고 저장소도 공개다. **값만 맞춰 두면 다음 사람이
"JWT 검증이 있으니 괜찮다" 로 읽을 여지가 남는다.**

---

# 4. service_role 함수 — 0031 이 1순위로 꼽은 항목 (pmdb #159)

## 4.1 정지·회수가 실제로 안 먹었다

DB 경로는 `app.uid()` 가 매 요청 `status='active'` 와 `token_version` 을 검사한다.
그런데 **JWT 를 직접 검증하고 service_role 로 진행하는 함수들은 서명과 `exp` 만 본다.**
RLS 를 우회하므로 그 관문을 거치지 않는다.

결과: 회수된 토큰과 **정지·차단된 계정**이 access 수명 동안(레거시 토큰이면 **30일**)
지역인증·사진인증·펫 신원등록·업체인증을 계속 할 수 있었다. 관리자가 계정을
정지시켜도 이 함수들에서는 아무 일도 일어나지 않고, 그 사실이 어디에도 표시되지 않는다.

**간이 토큰도 여기서만 뚫려 있었다.** §1.1 이 `app.uid()` 에 lite 클레임 거부를
넣었지만 이 함수들은 `app.uid()` 를 안 거치므로 그 방어가 적용되지 않았다 — 후기
전용 15분 토큰으로 펫 신원등록·지역인증이 됐다.

### 왜 이렇게 됐나

같은 JWT 검증 로직이 **여섯 벌 복제**돼 있었다. 사본이 여섯이면 드리프트는 시간
문제고, 실제로 그 사본들은 `_shared` 판과 달리 **`alg` 헤더 고정 확인조차 없었다.**

`invite-guardian:50` 의 주석 — *"호출자 상태(active) 확인 — service_role 경유라 직접
검증한다"* — 이 정답을 알고 있었는데 **그 함수만** 그렇게 하고 있었다.

### 조치

`_shared/auth.ts` 에 `activeUid()` 하나를 만들고 사본 여섯 개를 전부 제거했다.
`app.uid()` 와 같은 것을 본다: `status='active'` + `token_version == tv` +
**`lite` 클레임 없음**.

조회 실패는 **fail-closed** 다. `rateLimited` 의 fail-open 과 반대인데, 저쪽은
가용성 우선의 절충이고 이쪽은 **인증**이라 열어 둘 이유가 없다.

`verifyAccess` 만 쓰던 `apply-business`·`check-business-no` 도 같은 관문으로 옮겼다.

## 4.2 유료 외부 API 상한

전부 `rateLimited()` 를 쓰지 않고 있었다 — 로그인 사용자 한 명이 루프로 호출하면
그대로 비용이고, 앱 UI 는 한 번씩만 부르니 **실사용 관찰로는 영원히 안 보인다.**
§4.1 과 합치면 더 나빴다: **정지된 계정도 태울 수 있었다.**

| 버킷 | 값 | 대상 |
|---|---|---|
| `aiphoto:<uid>` | 30/시간 | Gemini 이미지 — 게시글 사진 인증 |
| `aienroll:<uid>` | 10/시간 | Gemini 영상 — 펫 신원 등록(호출당 비용 최대) |
| `geoloc:<uid>` | 30/시간 | Naver 역지오코딩 — 지역 인증(30일 주기) |
| `georev:<uid>` | 60/시간 | Naver 역지오코딩 — 작성 화면 현재 위치 안내 |
| `geosearch:<uid>` | 120/시간 | Naver 지역검색 — 지도 탭·이름 검색 |
| `dongsync:<uid>` | 5/시간 | 행정동 중심좌표 배치 |

값은 앱 코드에서 정상 호출 패턴을 확인하고 잡았다 — `geosearch` 가 유독 큰 건
지도 탭이 진입과 이름검색(250ms 디바운스)에서 부르기 때문이다.

## 4.3 간이 회원이 정식 가입을 못 했다 (pmdb #160)

`signup_user` 에는 승격 분기가 **실제로 구현돼 있다** — 같은 번호가 `status='lite'`
면 새 행을 만들지 않고 기존 행을 `active` 로 UPDATE 한다. **id 가 유지되므로 간이로
쓴 후기가 그대로 딸려 온다.**

그런데 `send-phone-code` 가 계정 존재를 **status 없이** 확인하고 `phone_taken` 을
돌려주는 바람에 signup 목적 인증을 영영 못 받아 **그 분기에 도달할 수가 없었다.**

앱은 `phone_taken` 에 *"로그인하거나 비밀번호 찾기를 이용해주세요"* 를 띄우는데
**둘 다 벽이다** — 간이 계정의 `password_hash` 는 검증 불가 sentinel `'!'` 이고,
재설정은 200 을 받아도 `get_login_user` 가 `status='active'` 만 돌려주므로 로그인이
안 된다.

조치: signup 목적 존재 검증에서 간이 계정을 제외. `password_reset` 도 막는다
(비밀번호가 없는 계정에 재설정을 통과시키면 200 을 받고도 로그인이 안 되는 상태가 된다).

---

# 5. 법무 — 기능은 프로덕션인데 문서에 없던 것들

## 5.1 간이 후기 비회원 전화번호 (pmlegal #18 · pmdart #259)

간이 후기는 **2026-07-27부터 프로덕션**이고 번호는 `users` 에 **평문 저장**된다.
그런데 처리방침에는 `간이`·`비회원`·`lite` 라는 말이 **한 번도 나오지 않았다.**
§1-1 수집 항목 표는 전화번호를 "회원가입(필수)" 과 "케어 리포트 수신 고객(HMAC)"
둘에만 귀속시켜, **정보주체 한 부류가 통째로 빠진 상태**였다(개인정보보호법 §30).

이용 시점에는 「간이 후기 이용조건」으로 별도 동의를 받고 있었으므로 **이용자 고지
자체가 없었던 것은 아니다.** 법정 공개 문서에만 반영이 안 돼 있었다.

### 약속을 먼저 이행하고 문서를 고쳤다 (pmdb #157)

이용조건 §3 은 *"후기를 모두 삭제하거나 파기를 요청하면 지체 없이 파기합니다"* 라고
약속하는데 **그 파기를 하는 코드가 어디에도 없었다.** `status='lite'` 참조는 전부
조회·표시용이고 정리 배치도 없었다.

인증만 받고 후기를 안 쓴 경우는 더 나쁘다 — `signup-lite` 는 토큰 발급 시점에 계정을
만들므로, 작성 화면에서 이탈하면 **아무 목적도 달성하지 못한 전화번호**가 남는다.

일일 retention 배치에 항목을 더했다. **행을 지우지 않고 전화번호를 NULL 로** 만든다:
`users` 참조 FK 가 30개 넘어 행 삭제는 부수효과가 크고, 간이 계정이 가진 개인정보는
번호 하나뿐이라(username·nickname 이 `lite_<랜덤hex>`) 번호를 지우면 남는 행은
식별력이 없다. `withdraw_account` 가 정식 회원에 쓰는 익명화와 같은 판단이다.

**지키지 않는 약속을 법정 문서에 한 줄 더 늘리는 셈이 되면 안 되므로 구현이 먼저였다.**

## 5.2 Cloudflare 수탁 · 앱 오류 진단 정보 (pmlegal #19 · pmdart #261)

`workers/share-proxy` 가 **자사 계정·자사 도메인**에서 공유 뷰어를 프록시하고 웹 앱은
Cloudflare Pages 로 배포된다. 공유 링크로 열리는 **케어 리포트(반려동물 호칭·돌봄
사진·돌봄 기록)와 게시물 내용**이 그 경로를 지난다. Supabase 앞단의 Cloudflare 는
재위탁으로 볼 여지가 있지만 이건 **직접 계약·배포한 독립 수탁자**다.

`app.client_errors` 는 실제 컬럼 그대로 적었다(오류 위치·메시지·스택·앱 버전·플랫폼·
회원 식별자). **접속 IP 는 저장하지 않는다**는 점을 명시했다 — `record_client_error`
가 `cf-connecting-ip` 를 레이트리밋 버킷 키(md5)로만 쓰고 테이블에는 남기지 않는다.
안 적으면 기존 "단말기 정보" 안에 IP 가 포함된다고 읽힐 수 있다.

## 5.3 마스킹 설명이 자기 예시를 부정했다 (pmlegal #14 · pmdart #260)

```
"앞 3자리는 전부 가리고, 가운데 4자리 중 첫 자리와 마지막 2자리만 표시합니다"
표시 예: ***-1***-**78
```

`78` 은 가운데 4자리(1234)가 아니라 **마지막 4자리(5678)의 끝 2자리**다. 운영에서
`app.mask_phone` 을 직접 돌려 확인했고, 11자리가 아닌 번호의 형태(`***-****-**67`)도
문서에 없어 함께 넣었다.

**법적 고지문에서 마스킹 범위를 잘못 적는 건 그 자체로 문제다** — 틀린 문장은
실제보다 **더 가려진다고** 읽힌다.

실제 표시 방식은 처음부터 바뀌지 않았으므로 개정이 아니라 표기 정정이고, 2026-07-27
시행을 유지하되 정정 사실을 문서 끝에 남겼다.

## 5.4 공개 저장소의 개인 신원 정보 — 이 문서의 유일한 미완

`pmlegal` 은 공개인데 `01사업계획서초안.md` 에 게시 의무가 없는 개인사업자 신원 정보
(대표 개인 휴대전화번호·사업장 주소·사업자등록번호)가 2026-07-02부터 한 달간 노출돼
있었다. 나머지 문서는 법정 게시 의무가 있어 공개가 맞고, 01 만 방통위 제출용이다.

`git filter-branch` 로 전체 ref 이력에서 제거하고 `.gitignore` 로 로컬 전용으로
돌렸다(세 값이 다른 파일 이력에는 없음을 전수 확인).

> ⚠️ **그것만으로는 공개가 취소되지 않았다.** GitHub 는 force push 후에도 도달 불가
> 객체를 즉시 수거하지 않는다 — **머지된 PR 페이지가 옛 커밋 SHA 를 그대로 공개하고,
> 그 SHA 로 파일 전체가 계속 조회됐다.** 저장소를 비공개로 돌려 차단했다가, 요청에
> 따라 다시 공개로 되돌렸다.
>
> **현재 상태**: `main` 에는 파일이 없지만 옛 SHA 로는 여전히 조회된다. 닫으려면
> GitHub 지원에 고아 객체 수거를 요청해야 한다(support.github.com/contact,
> "Remove cached views and unreachable Git objects"). **미완 항목이다.**

---

# 6. 검증 장치 — 초록불이 무엇을 보증하는지 좁혔다

## 6.1 배포가 테스트 통과와 무관하게 나갔다 (pmdb #152 · pmdart #254)

0031 §2.3 의 조치. `workflow_run` 게이팅으로 옮겼고, 첫 머지부터 정상 발화하는 것을
확인했다(0031 이 경고한 "첫 배포가 안 뜰 수 있다" 는 발생하지 않았다). paths 대체
로직도 동작한다 — 함수를 안 건드린 커밋에서 배포가 `skipped` 로 떨어졌다.

## 6.2 그런데 그 게이트가 함수를 보지 않았다 (pmdb #161)

`deploy-functions.yml` 은 "DB Tests" 성공에 물려 있는데 그 워크플로는 pgTAP 과
리플레이만 본다. **배포를 유발하는 변경(`supabase/functions/**`)과 게이트가 검증하는
대상(스키마)이 서로소였다.** 0031 은 §2.3 에서 게이팅을, §3.6 에서 타입 검사 부재를
따로 적었는데 **둘을 잇지 않았다.**

`deno check` 래칫(현재 18건 상한)을 DB Tests 에 넣어 연결했다. 0 이 아닌 이유는
18건을 한 번에 고칠 수 없어서고, 줄면 CI 가 상한을 내리라고 알려 준다.

⚠️ import 가 `jsr:@supabase/supabase-js@2` 처럼 범위 지정이고 lockfile 이 없어
**숫자가 우리 코드와 무관하게 움직일 수 있다.** lockfile 을 두지 않은 건 **배포가
그걸 쓰지 않기 때문**이다 — CI 만 고정하면 실제로 나가는 것과 다른 걸 검사하게 되고,
그건 이 문서가 내내 고쳐 온 부류가 된다. Deno 버전만 고정해 드리프트 축을 줄였다.

## 6.3 리플레이가 public·app 밖을 못 봤다 (pmdb #162)

`pg_dump -n public -n app` 이라 다음이 대조 밖이었다: `supabase_realtime` 퍼블리케이션
멤버십 · `storage.buckets` 설정 · **`storage.objects` RLS 정책(사용자 사진 접근 제어)** ·
**`cron.job`(보존기간 파기 배치)**.

즉 *"마이그레이션 없이 친 DDL 을 즉시 잡는다"* 는 보증이 **사진 접근 제어와 파기
배치에는 적용되지 않았다.** `scripts/backup.sh` 는 이 사각지대를 알고 복원 절차에
적어 두었는데 **CI 만 모르고 있었다.**

### 실제 드리프트가 있었다

운영 publication 에는 `chat_messages` 와 `notifications` 둘 다 있는데 마이그레이션에는
`chat_messages` 한 줄뿐이었다. 마이그레이션만으로 세운 DB(CI·재해복구·스테이징)에서는
앱의 `rt:notifications:<uid>` 구독이 **오류 없이 조용히 아무 이벤트도 못 받는다** —
구독은 성공으로 보이는데 벨 배지·알림 목록·포그라운드 알림이 전부 죽는다.

`dump_outofband_to()` 로 넷을 텍스트로 뽑아 `supabase/schema/outofband.txt` 에 두고
리플레이 DB 와 대조한다. **스냅샷 DB 가 아니라 커밋된 파일과 대는 이유**는
`schema.sql` 에 이 객체들이 아예 없어서 복원해도 아무것도 안 생기기 때문이다.

## 6.4 pgTAP 환경은 권한 회수를 표현하지 못한다 (t17 작업 중 발견)

`pg_dump` 는 GRANT 만 내보내고 REVOKE 는 안 내보내는데, pgtap 잡의 DB 에는 public
스키마에 `alter default privileges … grant all on tables to … authenticated` 가 이미
걸려 있다. CREATE TABLE 순간 ALL 이 붙고 뒤따르는 제한된 GRANT 는 얹을 게 없으므로
**회수한 권한이 복원본에서 되살아난다.**

`post_pets` 만의 이야기가 아니다 — `20260603053405`(anon 쓰기 회수) 같은 **회수
마이그레이션의 효과가 pgTAP 스냅샷 DB 에서 전부 되살아나 있다.** 즉 회수에 기대는
단언은 이 환경에서 **사실을 재지 못한다.**

조건부 skip 으로 우회해 봤지만 운영에도 같은 기본 권한이 있어 어디서도 실행되지 않는
죽은 분기가 됐다. 그래서 단언을 빼되 **왜 못 재는지와 어디서 재는지**를 파일에 남겼다.
정본 검증은 리플레이 잡이다.

## 6.5 브랜치 보호

pmdb 에는 룰셋이 **아예 없어** 필수 체크 없이 main 직접 푸시가 가능했다 — 방금 만든
`denocheck` 도 그냥 우회된다. 필수 체크 3종 + 삭제 금지로 신설했다.

pmdart 는 있었지만 `strict_required_status_checks_policy: false` 라 0031 §2.3 이
지적한 *"PR A + PR B 조합은 아무도 테스트하지 않았다"* 가 그대로였다. `true` 로 바꿔
브랜치 최신화를 요구하게 했다 — 이 세션에서 PR 을 머지할 때마다 손으로 하던 일이다.

force push 금지는 넣지 않았다. bypass 없이 걸면 긴급 상황에 되돌릴 방법이 없어지는데,
§5.4 에서 실제로 이력 재작성이 필요했던 걸 보면 그 여지는 남기는 게 낫다고 봤다.

## 6.6 매일 아침 9시간 실패하던 테스트 (pmdb #156)

`t13` 의 접종 리마인더 단언이 **코드 변경과 무관하게 시각만으로** 빨간불이 됐다.
준비는 `due_date = current_date`(서버 **UTC** 날짜)로 행을 만들고, 스윕은
`due_date >= (now() at time zone 'Asia/Seoul')::date` 로 거른다.

UTC 15:00~24:00 = **KST 00:00~09:00** 구간에서는 UTC 날짜가 KST 날짜보다 하루 뒤라
방금 만든 행이 자기 스윕에서 탈락한다. 즉 **매일 아침 9시간 동안만 실패**하는
테스트였고, 그동안의 CI 는 전부 그 창 밖에서 돌았다.

운영 크론은 무사하다 — 본문의 날짜 기준이 전부 `Asia/Seoul` 이고 `current_date` 를
쓰지 않는다. **테스트 준비 코드에만 있던 결함**이다.

---

# 7. 앱 — 조용히 죽는 경로

## 7.1 realtime 구독 실패가 영구화됐다 (pmdart #264)

`.subscribe()` 를 콜백 없이 불러 `CHANNEL_ERROR`·`TIMED_OUT` 을 몰랐다. 그런데
**첫 구독이 실패해도 `_notif` 에는 채널 객체가 들어가 non-null 이 된다** —
그래서 이후 모든 `start()` 가 `if (_notif != null) return;` 으로 조기 리턴하는
**no-op** 이 됐다. 로그인 직후든 포그라운드 복귀든 되살아나지 않는다.

**FCM 푸시는 별개 경로라 계속 온다.** 그래서 사용자에게는 *"가끔 배지가 안 맞네"*
정도로만 보인다 — 신고할 만한 증상이 아니고, 개발 중에는 구독이 실패할 일이 드물어
재현되지 않는다.

상태 콜백을 달고 끊기면 **채널을 반드시 비운 뒤** 백오프로 재구독한다(2·4·8·16·32초
→ **60초 상한**). 상한을 둔 이유: 무한히 벌어지면 복구가 사실상 안 되고, 그건 이
수정이 없애려는 상태(앱 재시작이 유일한 해법) 그 자체다.

구분해야 할 게 셋이라 상태를 셋 뒀다 — `_wanted`(우리가 끊었나 서버가 끊었나.
`removeChannel` 이 `closed` 콜백을 발화시키므로 없으면 **로그아웃 직후 되살아난다**),
`_gen`(끊긴 채널의 뒤늦은 콜백 무시. 없으면 **재시도가 이중으로** 잡힌다),
`_attempt`(백오프 + 첫 실패만 보고).

## 7.2 1000행 조용한 절단 (pmdart #266)

`db-max-rows = 1000` 이 걸려 있는데 `lib/services` 의 `.select()` 67곳 중
`.limit()`/`.range()` 는 7곳뿐이었다.

처음엔 "상한 없음(메모리 폭발)" 으로 보이지만 **그건 서버가 이미 막고 있다.** 대신
더 나쁜 게 남는다 — 댓글 1,200개인 글에서 1,000개만 와도 오류도 경고도 없고,
1001번째부터는 "실패" 가 아니라 **"존재하지 않음"** 으로 보인다.

이 프로젝트는 같은 실패 모드를 이미 겪고 이름을 붙였다 — #234, *"오류가 데이터처럼
보이는 최악의 실패 모드"*. 그때는 오류를 데이터로 착각했고 이건 **잘린 데이터를
전부라고 착각**한다.

**행 수를 늘리는 게 답이 아니다.** 상한은 서버가 정하고 늘려도 언젠가 다시 닿는다.
지금 진짜 문제는 **어느 목록이 안 닿는지, 이미 닿고 있는지조차 알 방법이 없다**는
것이라, 그걸 먼저 없앴다(`guardTruncation()`, 9곳 적용).

명시적 상한이 있는 곳은 감싸지 않았다 — 시설 후기(`p_limit:50`)와 `fetchFeed`
(`.limit(100)`, #30 이 추적 중)는 설계된 상한이지 조용한 절단이 아니다.
**감지지 페이지네이션이 아니며**, 실제로 닿는 경로가 나오면 그때가 후속 작업이다.

## 7.3 출시 직전 묶음 (pmdart #265)

**`firebase.json` 이 폐기된 `com.example` 앱을 가리켰다.** 출하 산출물끼리는 맞아서
지금은 무해하지만, 누가 `flutterfire configure` 를 돌리는 **순간** 그 앱 기준으로
재생성된다 — **APNs 키가 안 붙은 앱이라 푸시가 전멸**하고 산출물만 봐서는 안 보인다.

**Android 자동 백업이 켜져 있었다**(선언이 없어 기본값 true). `flutter_secure_storage`
가 백업에 들어가는데 **복호화 키인 Keystore 는 백업되지 않는다.** 새 기기로 복원하면
깨진 암호문을 읽고 로그인이 조용히 풀린다. `allowBackup="false"` 만으로는 기기 간
전송이 안 막혀 `data_extraction_rules.xml` 도 넣었다.

**release 서명이 debug 로 조용히 폴백했다.** 키 없는 머신에서도 릴리스 빌드가
성공하고, 그 산출물이 설치되면 정식 키 버전으로 **업그레이드가 막힌다**.

> 작업 중 실수를 잡았다 — 처음엔 `buildTypes.release` 블록 안에서 throw 했는데
> **그 블록은 설정 시점에 실행돼** 키 없는 머신의 debug 빌드와 `flutter run` 까지
> 막는다. `gradle.taskGraph.whenReady` 로 옮겼고, `buildTypes` 쪽은 폴백 대신
> `signingConfig = null`(미서명)로 둬서 검사를 우회해도 debug 서명본은 안 나가게 했다.

### `aps-environment` 는 일부러 바꾸지 않았다

0031 §5.7 은 이걸 "TestFlight 실기기로 확인해야 한다" 는 **검증 항목**으로 분류했지
확정 결함으로 두지 않았다. 배포 데이터로도 판정이 안 된다 — iOS 토큰 54개(활성 8),
최근 14일 푸시 7건 전부 `sent`·오류 0인데 `sent` 는 FCM 수락까지만 증명한다.

Xcode 는 배포용 프로비저닝으로 서명할 때 이 값을 프로파일 값으로 덮는 것이 일반적이라
지금 값이 문제가 아닐 가능성이 크고, **심사 중에 서명·entitlement 를 건드리는 건
위험**하며 여기서 빌드로 검증할 수단도 없다. **TestFlight 푸시 1회 수신 확인이 먼저다.**

---

# 8. 배포·검증 방법

모든 DB 변경은 운영에 적용한 뒤 SQL 을 저장소에 정리했고, 함수는 CI 배포로 나갔다.
검증에서 지킨 원칙 몇 가지를 남긴다 — 이번에 실제로 오판을 잡아낸 것들이다.

**초록불이 무엇을 봤는지 확인한다.** `denocheck` 가 9초 만에 통과했을 때 로그를 열어
"18건" 을 실제로 봤는지 확인했다(`|| true` 가 실패를 삼켜 0으로 통과했을 수 있다).
`replay` 도 5b/5 단계를 실제로 돌았는지 로그로 확인했다.

**비인증 상태로 잰다.** pmlegal 비공개 전환 후 `curl` 로 404 를 확인했다. 인증된
세션으로 보면 당연히 보이므로 판정이 안 된다.

**환경이 표현할 수 있는 것만 단언한다.** §6.4 가 그 사례다 — pgTAP 이 권한 회수를
표현하지 못하는데 단언을 넣으면 "무조건 통과하거나 무조건 실패" 가 되지 사실을 재지 않는다.

**못 잰 것은 못 잤다고 적는다.** realtime 상태 기계(웹소켓 필요), 간이 계정 승격
경로(운영에 lite 0건), `activeUid` 의 인증 경로(JWT_SECRET 없음 — 사용자가 지도 탭을
열어 확인) 등. 정황 증거가 충분해도 실측과 구분해 적었다.

## 7.8 푸시 실패 하나가 그 사람의 모든 기기를 껐다 (pmdb #170)

`send-push` 는 FCM 오류를 이렇게 읽었다:

```ts
if (code === "UNREGISTERED" || code === "INVALID_ARGUMENT" || r.status === 404) dead.push(t.token);
```

`INVALID_ARGUMENT` 는 **요청이 잘못됐다** 는 뜻이다. 토큰이 잘못돼도 나오지만
**페이로드가 잘못돼도** 나온다(제목·본문 타입, data 값이 문자열이 아님, 크기 초과 …).
페이로드 버그면 그 알림의 **모든 토큰**이 같은 오류를 받으므로, 배포 실수 한 번에
수신자의 기기가 전부 비활성화된다. **우리 잘못을 사용자 기기로 갚는 구조**였다.

비용이 한쪽으로만 크다:

| 판정 | 틀렸을 때 |
|---|---|
| 오탐(살아 있는 토큰을 끔) | 그 사람은 **그때부터 푸시를 못 받고 그 사실을 모른다** |
| 미탐(죽은 토큰을 남김) | FCM 호출 몇 번 낭비. 다음 UNREGISTERED 에서 정리된다 |

그래서 **토큰 문제라고 확신할 때만** 끄도록 바꿨다. FCM v1 은 잘못된 필드를
`details[].fieldViolations[].field` 로 알려 주는데, 토큰 문제면 그게 `message.token` 이다.
근거가 없으면 끄지 않는다 — 확신이 없을 때의 기본값은 '살려 둔다' 여야 한다.

분류를 순수 함수(`_shared/fcm.ts`)로 떼고 테스트 9건을 붙였다. 저장소 최초의 엣지 함수
단위 테스트라 CI 에 `deno test` 단계도 같이 넣었다.

또한 `THIRD_PARTY_AUTH_ERROR`(APNs 키 문제 — iOS 전체가 조용히 죽는다)와 토큰 무관
`INVALID_ARGUMENT` 는 재시도로 낫지 않으므로 관리자에게 바로 알린다. §7.5 의 운영 알람은
"실패 20건/15분" 이라 **저볼륨에서는 안 걸린다.**

### 고치다 보니 래칫 자체가 눈을 감고 있었다

타입 검사를 돌리니 오류가 18 → 8 로 줄었다(`edge_alert.ts` 의 `ReturnType<typeof
createClient>` 하나가 호출부마다 오류를 만들고 있었다). 같은 패턴을
`enroll-pet-identity` 에서도 지우니 **1건**이 됐는데, 여기서 이상한 걸 봤다.

**`deno check` 는 오류가 1건이면 `Found N errors` 줄을 찍지 않는다.**
오늘 아침 내가 만든 래칫은 그 줄을 파싱한다(0031 §3.6 방식). 그래서:

- 오류 1건 → `n=0` 으로 집계 → **통과**
- 게다가 "0건으로 줄었으니 MAX 를 0 으로 내리세요" 라는 안내까지 낸다
- 그대로 굳으면 게이트가 **1건짜리 오류에 영원히 눈을 감는다**

`TS####  [ERROR]` 헤더 줄을 세도록 바꾸고, 타입 오류가 아닌 실패(잘못된 인자·모듈
해석 실패 등)로 rc≠0 인데 집계가 0이면 실패 처리하는 백스톱을 넣었다. 두 갈래 모두
로컬에서 재현해 확인했다 — 1건짜리는 이제 1로 집계되고, `--bogus-flag` 같은 실패는
백스톱이 잡는다.

§6.2 와 같은 종류다. **게이트를 만들었으면 그 게이트가 실제로 재는지도 재야 한다.**
그때는 "함수를 아무도 안 본다" 였고, 이번엔 "1건은 못 센다" 였다.

## 7.7 공개 버킷이 임의 파일 호스팅이었다 (pmdb #169 · pmdart #268)

`media` 는 공개 버킷이고, 저장된 content-type 이 응답 헤더로 **그대로** 나간다.
실측으로 확인했다 — 공개 URL 응답에 `X-Content-Type-Options: nosniff` 도
`Content-Disposition` 도 없다.

그런데 버킷에 `allowed_mime_types` 도 `file_size_limit` 도 없었다. 쓰기 통제는 경로
규약(`<uid>/...`)뿐이라 **로그인한 누구나 자기 폴더에 무엇이든** 올릴 수 있었다:

- `text/html` → supabase.co 도메인에서 **살아 있는 HTML 페이지**(피싱 호스팅).
- `image/svg+xml` → 이미지처럼 보이지만 스크립트를 품는다.
- 크기 무제한 → 앱의 100MB 검사는 클라이언트 것이라 REST 직접 호출로 우회된다.

엣지 함수 두 곳(`enroll-pet-identity`·`verify-post-photo`)은 **클라이언트가 보낸
`mimeType` 을 그대로 `contentType` 으로** 넘기기까지 했다.

### 검증을 호출부가 아니라 버킷에 뒀다

업로드 경로가 여럿이다 — 앱 3곳(`upload`/`uploadBytes`/`uploadVideo`), 엣지 함수 2곳.
호출부마다 검사를 흩으면 §0 의 **부분 적용**을 또 만든다. **모든 업로드가 지나는 한
곳**은 버킷이므로 거기에 뒀다.

`image/*` 와일드카드는 쓰지 않았다 — **`image/svg+xml` 을 함께 통과시키는데 와일드카드로는
뺄 수가 없다.** 명시 목록의 근거는 각각 있다: jpeg/png 는 현재 운영 객체 전부(58 + 21건),
webp/gif 는 갤러리 경로, **heic/heif 는 `_normalizePhoto` 가 디코드 실패 시 원본을 그대로
올리기 때문**(빼면 비-Safari HEIC 사용자만 조용히 깨진다), 영상 4종은 iOS·안드로이드
갤러리 컨테이너.

### 그래도 두 겹을 더 뒀다 — 이유는 보안이 아니다

- **엣지 함수**: 클라이언트 mimeType 을 먼저 검사해 400 으로 돌려준다. 없으면 스토리지가
  415 로 튕기고, 함수는 그걸 "업로드 실패" 로 읽어 **500 + 관리자 알람**을 울린다.
  §7.5 에서 알람을 막 만들었는데 잘못된 입력이 알람을 울리면 안 된다.
- **클라이언트**: 같은 목록으로 미리 걸러 사람 말로 이유를 준다. 없으면 화면에는
  "업로드 실패" 만 남는다.

두 사본이 버킷 목록과 어긋날 수 있다는 게 이 구조의 비용이다. 주석에 "넓히는 방향으로
어긋나면 안 된다" 고 적고, 클라이언트 쪽은 테스트 11건으로 목록을 고정했다.

### 검증

운영에서는 service_role 키가 없어 인증 업로드를 직접 재볼 수 없었다. **테스트 프로젝트에
격리된 버킷(`mimetest`, 허용=image/jpeg, 상한 1MB)을 만들어** Supabase 가 실제로 막는지
확인하고 정리했다:

| 시도 | 결과 |
|---|---|
| `text/html` | **415 InvalidMimeType** — 차단 |
| `image/svg+xml` | **415 InvalidMimeType** — 차단 |
| `image/jpeg` | 200 — 통과 |
| `image/jpeg` 2MB(상한 1MB) | **413 EntityTooLarge** — 차단 |

정리 중에 `storage.protect_delete` 트리거가 직접 삭제를 막아 `set local
storage.allow_delete_query = 'true'` 로 해제하고 지웠다. 테스트 프로젝트에 남은 것은 없다.

## 7.6 초대 열거 — 리밋이 걸려 있지 않은 쪽으로 새고 있었다 (pmdb #168 · pmdart #267)

`invite-guardian` 은 응답에 `registered: true|false` 를 실어 보냈다. 번호만 바꿔 부르면
**"이 번호가 PawMate 회원인가" 를 무제한으로 캐낼 수 있는 오라클**이다.

리밋이 없었던 게 아니라 **엉뚱한 쪽에만 있었다.** 코드 순서가 이랬다:

```
if (target) return json({ ok: true, registered: true });   // ← 가입자면 여기서 끝
const limited = await rateLimited(...ginv:u...)            // ← 리밋은 그 다음
```

가입자 번호를 넣으면 **리밋을 만나기 전에 반환**된다. 즉 열거의 성공 케이스는
비용이 0이었고, 실패 케이스(미가입)만 하루 10건으로 묶여 있었다. 0031 §3.4 가 완화
근거로 든 `sms:ip` 도 이 경로엔 걸리지 않는다.

### 그런데 고칠 곳은 엣지 함수가 아니었다

리밋을 분기 앞으로 옮기려다, 그 리밋이 **애초에 우회 가능**하다는 걸 봤다.
`pgi_insert` 정책이 인증 사용자에게 직접 INSERT 를 허용한다 — 펫 owner 면 PostgREST 로
초대 행을 바로 넣을 수 있고, BEFORE 트리거가 번호로 가입자를 찾아 `invitee_user_id` 를
채우고, `pgi_select` 가 초대자에게 그 행을 보여 준다. **엣지 함수를 아예 안 거치고,
리밋도 없이, 같은 사실을 캐낼 수 있었다.**

함수에만 건 검증은 검증이 아니다 — 자기초대 차단 때 배운 것과 같은 구조인데
([[edge-function-validation-bypass]]), 이번에는 그걸 알고도 함수부터 고치려 했다.

실사용 확인 후 `revoke insert ... from authenticated, anon`:
앱은 이 테이블에 INSERT 하지 않고(초대는 전부 엣지 함수 경유), `kind='request'` 흐름은
설계만 있고 UI 가 없다(운영 0건). 운영 전체 초대 행은 1건뿐이었다. 정책은 지우지 않고
남겼다 — 지금은 도달할 수 없지만 그랜트가 되살아나는 날에도 조건은 남아 있어야 한다.

### 세 겹으로 닫았다

| 겹 | 무엇 |
|---|---|
| DB | 직접 INSERT 회수 → 모든 초대가 엣지 함수를 지난다 |
| 엣지 함수 | **분기 앞에서** 모든 시도를 20건/일·초대자로 센다 |
| 응답 | 가입 여부와 무관하게 `{ok:true}` — 예산 안에서도 덜 알려 준다 |

클라이언트는 문구를 가르지 않는다 — "초대했어요 — 상대가 가입 전이면 문자로 안내가
가요" 하나로 통일했다. 이게 UX 손실인가 보면, 초대자가 실제로 할 일은 어느 쪽이든
같다(기다리기). 갈라진 문구가 준 건 편의가 아니라 정보였다.

**남는 것**: 초대자는 자기가 만든 초대 행을 REST 로 읽어 `invitee_user_id` 유무로 같은
사실을 알 수 있다. 다만 행을 만들려면 엣지 함수를 지나야 하므로 **하루 20건으로 함께
묶인다**. 그리고 미가입 경로만 SMS 를 보내므로 응답 시간에 차이가 남는다 — 예산 안에서
타이밍을 재면 구분이 가능하다. 둘 다 "무제한 → 20건/일" 로 바뀐 것이지 0이 된 건 아니다.

## 7.5 관측 — 모으기만 하고 알려 주지 않던 것 (pmdb #167)

§9.3 이 지목한 두 가지를 함께 닫는다. 둘은 같은 병이다 — **자료는 쌓이는데 사람에게
도달하는 경로가 없다.** 그리고 이 병의 증상은 "틀린 알람" 이 아니라 **"아무 알람도 안 옴"**
이라서, 평상시(아무 일도 없을 때)와 겉모습이 같다. 출시 후에는 그대로 "문제가 있었는지조차
모름" 이 된다.

### 레이트리밋은 발동해도 흔적이 없었다

`rate_limit_hit` 은 카운트를 세고 `count <= max` 만 돌려줬다. 초과했다는 사실은 어디에도
남지 않고, 버킷 행은 만료되면 지워진다. 오늘 버킷을 8개 더 늘린 뒤라 더 급했다.

`app.rate_limit_trips(family, minute, trips)` 에 **막힌 것만** 적는다.

- `family` 는 키의 첫 토큰(`login:ip:1.2.3.4` → `login`). 개별 식별자는 **일부러 버린다** —
  "어느 계열이 얼마나 막혔나" 면 충분하고, 누가 막혔는지까지 남기면 보관할 이유 없는
  개인정보가 된다.
- 분 단위로 접는다. 공격 중에는 발동이 초당 수백 건이라 발동마다 한 행이면 관측하려다
  테이블을 채운다.
- 그 INSERT 는 **예외를 삼킨다.** 관측이 제한기를 죽이면 안 된다.

### 알림 경로는 이미 있는 것을 쓴다

`app.ops_alarm_sweep()` 이 5분마다 여섯 가지를 본다 — 지점별 앱 오류 급증 / 총량 급증 /
레이트리밋 계열별 다발 / 푸시 실패 / 크론 실패 / 증빙 파기 적체. 걸리면 `notifications` 에
`system_notice` 를 넣고, 그러면 기존 트리거·Edge Function·FCM 이 그대로 실어 나른다.
새 외부 서비스가 없으니 처리방침도 그대로다.

**지점별과 총량을 둘 다 보는 이유**: 지점별만 보면 서버가 죽어 모든 화면이 조금씩 실패하는
경우를 놓친다(지점별로는 임계 미달, 총량은 폭증). 총량만 보면 한 화면이 터진 걸 평상시
잡음에 묻는다.

**쿨다운(60분)이 필요한 이유**: 급증은 몇 분간 이어진다. 5분마다 같은 알람이 오면 사람은
알림을 꺼 버리고, 그러면 관측 장치가 없는 것과 같아진다.

**증빙 파기만 임계가 1이다.** 급증이 아니라 법정 의무 미이행이라서다 — §9.2 의
`purge-business-docs` 무알림 재시도 항목이 이걸로 관측된다.

### 설계하다 스스로 만든 조용한 실패 모드 둘

① **설정 행이 없으면 스윕이 조용히 0을 냈다.** `insert into ops_alarm_config` 는 스키마가
아니라 **데이터**라 `pg_dump --schema-only` 에 안 담긴다 → 스냅샷으로 세운 DB(CI·재해복구)
에서는 테이블이 비어 있다. 거기서 "행이 없으니 return 0" 이면 알람이 죽은 걸 알람이
알려 줄 수 없다. 기본값 행을 다시 넣고 진행하게 고쳤다.

② **`cron.job_run_details` 를 그냥 참조하면 pgTAP 환경에서 함수가 통째로 터진다.**
pg_cron 은 운영에만 있고 pgtap 잡은 `prelude + schema.sql`(= `-n public -n app` 덤프)만
복원한다. `to_regclass` 로 존재를 먼저 확인해 그 조건만 건너뛴다.

**남는 한계**: pg_cron 자체가 멈추면 이 스윕도 안 돈다 — 자기 자신의 부재는 감지 못 한다.
푸시 파이프라인이 죽어도 푸시 알람은 못 온다 — 그래서 알람을 이력(`app.ops_alarms`)·
앱 내 알림·푸시 **세 군데**에 남기고, 조회용 `admin_ops_alarms` 를 뒀다.

검증은 운영 DB 직행 롤백으로 12건(t21). "울려야 할 때 울리는가" 뿐 아니라 "설정이 없어도
살아 있는가"·"쿨다운이 지나면 다시 울리는가"·"끄면 꺼지는가" 를 함께 못 박았다.

## 7.4 문서를 세다가 나온 권한 구멍 (pmdb #164)

`docs/supabase-db.md` 를 실측으로 갱신하면서 카탈로그와 문서를 스크립트로 대조했다.
문서가 크게 뒤처져 있었고(트리거 52→77, 함수 54→108, RLS 76→83, 마이그레이션 77→192),
**서술이 정반대인 것도 있었다** — `business_profiles` 를 "전체 공개 + 본인/관리자 쓰기"
라고 적어 뒀는데 실제 정책은 `select_own` 하나뿐이다. 공개는 definer 뷰가, 쓰기는 RPC 가
한다. 문서만 보고 "테이블이 열려 있다" 고 판단하면 정반대의 결론에 도달한다.

세는 과정에서 나온 것이 이번 항목이다.

### posts.actual_lat / actual_lng — 안 받겠다고 한 값이 들어올 수 있었다

게시글 위치는 작성 GPS 가 아니라 **작성자 지역인증(`users.region_code`)** 기준이고,
정확 좌표는 저장하지 않는 게 정책이다. SELECT 컬럼 그랜트에도 두 컬럼은 없다.

그런데 **UPDATE 그랜트에는 남아 있었다.** 인증 사용자가 자기 글에 PATCH 로 정확 좌표를
써 넣을 수 있었다는 뜻이다.

읽히지 않으니 무해해 보이지만 그 반대다:

- `log_location_usage` 는 AFTER **INSERT** 라 UPDATE 를 안 본다 → **위치 이용 기록에 안 남는다.**
- 어떤 뷰·RPC 로도 노출되지 않는다 → **아무도 모른 채 쌓인다.**

즉 "수집하지 않는다" 고 공지한 값이, 수집한 사실조차 남기지 않은 채 저장될 수 있었다.
읽기가 막혀 있다는 게 오히려 발견을 늦춘다.

**§0 의 다섯 번째 유형(부분 적용) 그대로다.** 방어(컬럼 그랜트)는 있었고 SELECT 경로에는
제대로 걸려 있었다. 빠진 건 UPDATE 하나였고, 그게 평상시에 쓰이지 않는 경로라 아무도
밟지 않았다.

앱 코드에 `actual_lat`/`actual_lng` 참조가 없고 적용 시점 저장 행이 0건임을 확인한 뒤
`20260804140000` 으로 회수했다. 값을 채우는 `create_post_verified` 는 SECURITY DEFINER 라
이 그랜트가 필요 없다.

**베이스라인 처리**: 기반 스키마에서 온 그랜트라 어느 마이그레이션도 GRANT 하지 않는다.
스냅샷에서 빠진 뒤로는 `build_baseline.py` 가 역산할 근거가 없어(회수만 있고 부여가 없다)
`baseline-manual.sql` 로 옮겼다 — 세 번째 수기 조각이다. 이게 없으면 리플레이의 revoke 가
없는 권한을 회수하는 no-op 이 되고, 회수했다는 사실이 재현 경로에서 사라진다.

**pgTAP 은 넣지 않았다** — §6.4 와 같은 이유. 검증은 replay 잡이 한다(스키마 10,152줄 +
대역 외 20줄 대조 통과).

## 숫자

| | 값 |
|---|---|
| 마이그레이션 신규 | 10건 (`20260803180000` ~ `20260804200000`) |
| `deno check` 래칫 | 18 → **1** (계수 방식 결함도 수정) |
| 엣지 함수 단위 테스트 | (없음) → **9건** (저장소 최초) |
| pgTAP 파일 | 16 → **21** (t17~t21 신설, 단언 41건) |
| Flutter 테스트 | 255 → **304** |
| `catch (_)` 래칫 | 110 → **104**(현재 104, 여유 0) |
| `deno check` 래칫 | (없음) → **≤18** |
| 머지 PR | pmdb 18 · pmdart 15 · pmlegal 3 |

---

# 9. 남은 것

## 9.1 사람이 해야 하는 것

1. **TestFlight 실기기 푸시 1회 수신** — §7.3 `aps-environment` 판정의 유일한 근거
2. **GitHub 지원에 고아 객체 수거 요청** — §5.4. 요청 전까지 옛 SHA 로 사업계획서 조회 가능
3. **심사 통과 후 `DEMO_PHONES`/`DEMO_OTP` 해제** — §3 으로 백도어 표면이 두 함수로 늘었다
4. **08-10(이용약관)·08-11(처리방침) 시행일 전 새 빌드 판단** — 약관이 전부 번들 에셋이라
   개정 반영에 스토어 심사가 필요하다
5. **GCP 콘솔** — Firebase API 키 3개의 애플리케이션·API 제한 확인(0031 §4.2)

## 9.2 코드 — 중간 규모

- **차단당한 쪽의 REST 직접 열람** — `posts_select`·`comments_select` RLS 에 필터가
  없어 글을 **읽을** 수는 있다(쓰기·알림은 §2 에서 막았다)
- ~~**`send-push` 의 `INVALID_ARGUMENT` 오탐**~~ — **해소(#170, §7.8)**. 토큰 필드를
  지목했을 때만 끈다. 곁가지로 래칫 계수 결함(1건을 0으로 셌다)도 같이 고쳤다
- ~~**`purge-business-docs` 무알림 무한 재시도**~~ — **관측 확보(#167 §7.5)**. 24시간 넘게
  안 지워진 증빙이 1건이라도 있으면 알람이 울린다. 재시도 로직 자체는 그대로다
- ~~**`invite-guardian` 계정 열거**~~ — **해소(#168, §7.6)**. 원인은 리밋 위치였고, 고치려다
  **직접 INSERT 로 리밋 자체를 우회할 수 있다**는 걸 발견해 DB 에서 먼저 닫았다
- ~~**`media` 버킷에 MIME·용량 제한 없음**~~ — **해소(#169, §7.7)**. 버킷에 허용 MIME
  10종 + 100MB 상한. 남는 것: MIME 은 **선언값**만 검사되고(선언은 jpeg, 바이트는 다른 것),
  공개 버킷이라 URL 을 아는 누구나 원본을 볼 수 있다는 점은 그대로
- **RLS 76개 전부 `app.uid()` bare 호출** — `(select app.uid())` 래핑 0건, 행마다 재평가
- **인덱스 2건** — `reviews.reviewer_id`, `facility_reviews.user_id`(활동 화면 상시 경로)

## 9.3 관측

- ~~**오류를 모으는데 알려 주는 장치가 없다**~~ · ~~**레이트리밋 발동이 관측되지 않는다**~~
  — **해소(#167, §7.5)**. 5분 스윕이 여섯 조건을 보고 관리자에게 `system_notice` 로 알린다.
  §7.2 의 절단 감지도 `client_errors` 를 타므로 같이 덮인다.
- 남는 것: **pg_cron 이 멈추면 스윕도 안 돈다**(자기 부재는 감지 불가) · 알람 임계값이
  출시 전 추정치라 실트래픽을 보고 조정해야 한다(`app.ops_alarm_config`, 배포 없이 가능)

## 9.4 기존 백로그

pmdart #30 · #155~#160 (God Widget · 상태관리 · i18n · 커버리지 · `catch (_)` 분류) ·
pmdb #135(섀도 전환) · #136 · #149 · #150.

---

# 10. 이 문서가 만든 부채

- ~~**`docs/supabase-db.md` 가 2026-07-02 기준으로 여전히 낡았다**~~ — **해소(#164, §7.4)**.
  카탈로그 대조로 다시 세웠고 미기재 객체 0을 확인했다. 낡아 있던 정도는 예상보다 컸다
  — 트리거 52→77, 함수 54→108, 마이그레이션 77→192, 그리고 `business_profiles` 는
  서술이 정반대였다.
- **`docs/supabase-api.md` §4 의 레이트리밋 표**가 이번에 추가한 버킷 8개를 반영하지
  않았다(0031 §7.3 이 이미 지목한 항목의 잔여분).
- **정보주체 권리 조항의 메뉴 경로**가 실제와 다르다 — 처리방침·이용약관이
  "설정 → **계정** → 회원 탈퇴" 라고 적는데 실제는 '계정' 단계가 없다. §5 에서
  비회원 경로만 보완하고 이건 그대로 뒀다.
- **`assets/images/IMG_2962.png`** 가 세션 내내 미추적으로 남아 있다. 어디서도
  참조되지 않아 판단을 미뤘다.
